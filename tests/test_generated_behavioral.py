"""tests/test_generated_behavioral.py
AUTO-GENERATED by ms-testgen on 2026-05-04 13:39:03
DO NOT EDIT MANUALLY — re-run ms-testgen to update.

This file provides behavioral tests for nodes identified by ms-coverage
as uncovered or lacking runtime verification. Each test:
  - Executes real code (no source-text grepping)
  - Asserts on outcomes (return values, DB state, response shape)
  - Tests failure paths where applicable
  - Is tagged with the coverage node that generated it
"""
from __future__ import annotations
import json
import sys
import pytest
from pathlib import Path
from unittest.mock import MagicMock, patch

REPO = Path(__file__).resolve().parent.parent  # tests/ -> repo root


@pytest.fixture
def test_db(tmp_path):
    from backend.core.state import init_db
    from backend.core import state as state_mod
    db_path = tmp_path / "state.db"
    init_db(db_path)
    state_mod.configure(db_path)
    yield db_path
    state_mod.configure(None)  # Ensure cleanup even on test failure


@pytest.fixture
def app_client(tmp_path):
    """Self-contained fixture: creates its own isolated DB, never depends on conftest test_db."""
    from fastapi.testclient import TestClient
    from backend.core import state as state_mod
    from backend.core.state import init_db
    db_path = tmp_path / "gen_state.db"
    init_db(db_path)
    state_mod.configure(db_path)
    def _test_init(path):
        init_db(db_path)
        state_mod.configure(db_path)
    with patch("backend.api.main.init_db", side_effect=_test_init), \
         patch("backend.health.scheduler.start_scheduler"), \
         patch("backend.health.source_checker.run_source_scan", return_value=None):
        from backend.api.main import app
        with TestClient(app, base_url="http://localhost", raise_server_exceptions=False) as client:
            yield client, tmp_path
    state_mod.configure(None)  # Always clean up after ourselves


class TestGeneratedRoutes:
    """Auto-generated behavioral tests for API routes.
Each test verifies: no 500 error, response is JSON, failure paths return 4xx."""

    def test_route_GET__api_apps_key_responds(self, app_client):
        """GET /api/apps/{key} must respond without 500.
        Risk=low. handler=get_app
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/apps/sonarr")
        assert r.status_code != 500, (
            f"GET /api/apps/{{key}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/apps/{{key}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_apps_key_install_responds(self, app_client):
        """POST /api/apps/{key}/install must respond without 500.
        Risk=high. handler=api_install
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/apps/sonarr/install", json={})
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/install returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_GET__api_apps_key_install_progress_responds(self, app_client):
        """GET /api/apps/{key}/install/progress must respond without 500.
        Risk=high. handler=api_install_progress
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/apps/sonarr/install/progress")
        assert r.status_code != 500, (
            f"GET /api/apps/{{key}}/install/progress returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/apps/{{key}}/install/progress unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_DELETE__api_apps_key_responds(self, app_client):
        """DELETE /api/apps/{key} must respond without 500.
        Risk=medium. handler=api_remove
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.delete("/api/apps/sonarr")
        assert r.status_code != 500, f"DELETE /api/apps/{{key}} must not return 500"

    def test_route_POST__api_apps_key_replace_new_key_responds(self, app_client):
        """POST /api/apps/{key}/replace/{new_key} must respond without 500.
        Risk=medium. handler=api_replace
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/replace/{new_key}", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/replace/{{new_key}} returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_apps_key_restart_responds(self, app_client):
        """POST /api/apps/{key}/restart must respond without 500.
        Risk=medium. handler=api_restart
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/restart", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/restart returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_apps_key_logs_responds(self, app_client):
        """GET /api/apps/{key}/logs must respond without 500.
        Risk=low. handler=api_logs
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/apps/sonarr/logs")
        assert r.status_code != 500, (
            f"GET /api/apps/{{key}}/logs returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/apps/{{key}}/logs unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_apps_key_disable_responds(self, app_client):
        """POST /api/apps/{key}/disable must respond without 500.
        Risk=medium. handler=api_disable
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/disable", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/disable returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_apps_key_enable_responds(self, app_client):
        """POST /api/apps/{key}/enable must respond without 500.
        Risk=medium. handler=api_enable
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/enable", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/enable returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_apps_key_criticality_responds(self, app_client):
        """GET /api/apps/{key}/criticality must respond without 500.
        Risk=low. handler=api_criticality
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/apps/sonarr/criticality")
        assert r.status_code != 500, (
            f"GET /api/apps/{{key}}/criticality returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/apps/{{key}}/criticality unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_apps_system_profile_responds(self, app_client):
        """GET /api/apps/system/profile must respond without 500.
        Risk=low. handler=api_system_profile
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/apps/system/profile")
        assert r.status_code != 500, (
            f"GET /api/apps/system/profile returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/apps/system/profile unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_apps_batch_preflight_responds(self, app_client):
        """POST /api/apps/batch/preflight must respond without 500.
        Risk=medium. handler=batch_preflight
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/batch/preflight", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/batch/preflight returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_apps_batch_install_responds(self, app_client):
        """POST /api/apps/batch/install must respond without 500.
        Risk=high. handler=batch_install
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/apps/batch/install", json={})
        assert r.status_code != 500, (
            f"POST /api/apps/batch/install returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_POST__api_apps_lint_compose_responds(self, app_client):
        """POST /api/apps/lint-compose must respond without 500.
        Risk=medium. handler=lint_compose_yaml
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/lint-compose", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/lint-compose returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_apps_install_from_github_responds(self, app_client):
        """POST /api/apps/install-from-github must respond without 500.
        Risk=high. handler=install_from_github
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/apps/install-from-github", json={})
        assert r.status_code != 500, (
            f"POST /api/apps/install-from-github returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_POST__api_apps_install_custom_responds(self, app_client):
        """POST /api/apps/install-custom must respond without 500.
        Risk=high. handler=install_custom_app
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/apps/install-custom", json={})
        assert r.status_code != 500, (
            f"POST /api/apps/install-custom returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_PUT__api_apps_key_pin_version_responds(self, app_client):
        """PUT /api/apps/{key}/pin-version must respond without 500.
        Risk=medium. handler=pin_app_version
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/apps/sonarr/pin-version")
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/apps/{{key}}/pin-version returned 500 on minimal body: {r.text[:200]}"
        )
        # No body → must reject with 422, not crash with 500
        r_empty = client.put("/api/apps/sonarr/pin-version")
        assert r_empty.status_code in (200, 204, 400, 405, 409, 422), (
            f"Missing body should get 4xx or 405 not 500, got {r_empty.status_code}"
        )

    def test_route_POST__api_apps_key_update_responds(self, app_client):
        """POST /api/apps/{key}/update must respond without 500.
        Risk=medium. handler=update_app
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/update", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/update returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_apps_key_config_responds(self, app_client):
        """GET /api/apps/{key}/config must respond without 500.
        Risk=low. handler=get_app_config
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/apps/sonarr/config")
        assert r.status_code != 500, (
            f"GET /api/apps/{{key}}/config returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/apps/{{key}}/config unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_PUT__api_apps_key_config_responds(self, app_client):
        """PUT /api/apps/{key}/config must respond without 500.
        Risk=medium. handler=update_app_config
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/apps/sonarr/config")
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/apps/{{key}}/config returned 500 on minimal body: {r.text[:200]}"
        )
        # No body → must reject with 422, not crash with 500
        r_empty = client.put("/api/apps/sonarr/config")
        assert r_empty.status_code in (200, 204, 400, 405, 409, 422), (
            f"Missing body should get 4xx or 405 not 500, got {r_empty.status_code}"
        )

    def test_route_POST__api_apps_key_pull_responds(self, app_client):
        """POST /api/apps/{key}/pull must respond without 500.
        Risk=medium. handler=pin_tag_alias
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/pull", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/pull returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_apps_key_pin_tag_responds(self, app_client):
        """POST /api/apps/{key}/pin-tag must respond without 500.
        Risk=medium. handler=pin_tag_alias
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/pin-tag", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/pin-tag returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_apps_key_post_install_steps_responds(self, app_client):
        """GET /api/apps/{key}/post-install-steps must respond without 500.
        Risk=high. handler=get_post_install_steps
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/apps/sonarr/post-install-steps")
        assert r.status_code != 500, (
            f"GET /api/apps/{{key}}/post-install-steps returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/apps/{{key}}/post-install-steps unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_apps_key_check_update_responds(self, app_client):
        """GET /api/apps/{key}/check-update must respond without 500.
        Risk=low. handler=check_app_update
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/apps/sonarr/check-update")
        assert r.status_code != 500, (
            f"GET /api/apps/{{key}}/check-update returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/apps/{{key}}/check-update unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_apps_key_probe_path_responds(self, app_client):
        """POST /api/apps/{key}/probe-path must respond without 500.
        Risk=medium. handler=probe_health_path
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/probe-path", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/probe-path returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_apps_key_enhance_responds(self, app_client):
        """POST /api/apps/{key}/enhance must respond without 500.
        Risk=medium. handler=enhance_custom_app
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/apps/sonarr/enhance", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/apps/{{key}}/enhance returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_catalog_key_responds(self, app_client):
        """GET /api/catalog/{key} must respond without 500.
        Risk=low. handler=get_catalog_entry
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/catalog/sonarr")
        assert r.status_code != 500, (
            f"GET /api/catalog/{{key}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/catalog/{{key}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_health_apps_responds(self, app_client):
        """GET /api/health/apps must respond without 500.
        Risk=low. handler=get_all_app_health
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/apps")
        assert r.status_code != 500, (
            f"GET /api/health/apps returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/apps unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_health_apps_key_responds(self, app_client):
        """GET /api/health/apps/{key} must respond without 500.
        Risk=low. handler=get_app_health
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/apps/sonarr")
        assert r.status_code != 500, (
            f"GET /api/health/apps/{{key}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/apps/{{key}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_health_summary_responds(self, app_client):
        """GET /api/health/summary must respond without 500.
        Risk=low. handler=get_health_summary
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/summary")
        assert r.status_code != 500, (
            f"GET /api/health/summary returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/summary unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_health_llm_agent_responds(self, app_client):
        """GET /api/health/llm-agent must respond without 500.
        Risk=low. handler=get_llm_agent_status
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/llm-agent")
        assert r.status_code != 500, (
            f"GET /api/health/llm-agent returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/llm-agent unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_run_responds(self, app_client):
        """POST /api/health/run must respond without 500.
        Risk=medium. handler=trigger_health_run
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/run", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/run returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_scheduler_responds(self, app_client):
        """GET /api/health/scheduler must respond without 500.
        Risk=low. handler=get_scheduler_status
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/scheduler")
        assert r.status_code != 500, (
            f"GET /api/health/scheduler returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/scheduler unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_scheduler_pause_responds(self, app_client):
        """POST /api/health/scheduler/pause must respond without 500.
        Risk=medium. handler=pause_scheduler
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/scheduler/pause", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/scheduler/pause returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_PUT__api_health_settings_responds(self, app_client):
        """PUT /api/health/settings must respond without 500.
        Risk=medium. handler=update_health_settings
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/health/settings", json={"health_check_interval_secs": 60})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/health/settings returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_pending_actions_responds(self, app_client):
        """GET /api/health/pending-actions must respond without 500.
        Risk=low. handler=get_pending_actions
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/pending-actions")
        assert r.status_code != 500, (
            f"GET /api/health/pending-actions returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/pending-actions unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_health_anomalies_responds(self, app_client):
        """GET /api/health/anomalies must respond without 500.
        Risk=low. handler=get_anomalies
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/anomalies")
        assert r.status_code != 500, (
            f"GET /api/health/anomalies returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/anomalies unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_platform_review_responds(self, app_client):
        """POST /api/health/platform-review must respond without 500.
        Risk=medium. handler=run_platform_review
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/platform-review", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/platform-review returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_health_apply_fix_responds(self, app_client):
        """POST /api/health/apply-fix must respond without 500.
        Risk=medium. handler=apply_suggested_fix
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/apply-fix", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/apply-fix returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_ghost_resources_responds(self, app_client):
        """GET /api/health/ghost-resources must respond without 500.
        Risk=low. handler=get_ghost_resources
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/ghost-resources")
        assert r.status_code != 500, (
            f"GET /api/health/ghost-resources returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/ghost-resources unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_ghost_resources_action_responds(self, app_client):
        """POST /api/health/ghost-resources/action must respond without 500.
        Risk=medium. handler=handle_ghost_resource
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/ghost-resources/action", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/ghost-resources/action returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_weekly_summary_responds(self, app_client):
        """GET /api/health/weekly-summary must respond without 500.
        Risk=low. handler=get_weekly_summary
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/weekly-summary")
        assert r.status_code != 500, (
            f"GET /api/health/weekly-summary returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/weekly-summary unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_test_results_responds(self, app_client):
        """POST /api/health/test-results must respond without 500.
        Risk=medium. handler=record_test_results
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/test-results", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/test-results returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_test_results_responds(self, app_client):
        """GET /api/health/test-results must respond without 500.
        Risk=low. handler=get_test_results
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/test-results")
        assert r.status_code != 500, (
            f"GET /api/health/test-results returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/test-results unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_propose_tests_responds(self, app_client):
        """POST /api/health/propose-tests must respond without 500.
        Risk=medium. handler=propose_tests
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/propose-tests", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/propose-tests returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_proposed_tests_responds(self, app_client):
        """GET /api/health/proposed-tests must respond without 500.
        Risk=low. handler=list_proposed_tests
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/proposed-tests")
        assert r.status_code != 500, (
            f"GET /api/health/proposed-tests returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/proposed-tests unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_proposed_tests_proposal_id_approve_responds(self, app_client):
        """POST /api/health/proposed-tests/{proposal_id}/approve must respond without 500.
        Risk=critical. handler=approve_proposed_test
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/health/proposed-tests/{proposal_id}/approve", json={})
        assert r.status_code != 500, (
            f"POST /api/health/proposed-tests/{{proposal_id}}/approve returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_DELETE__api_health_proposed_tests_proposal_id_responds(self, app_client):
        """DELETE /api/health/proposed-tests/{proposal_id} must respond without 500.
        Risk=medium. handler=discard_proposed_test
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.delete("/api/health/proposed-tests/{proposal_id}")
        assert r.status_code != 500, f"DELETE /api/health/proposed-tests/{{proposal_id}} must not return 500"

    def test_route_POST__api_health_anomalies_app_key_check_name_snooze_responds(self, app_client):
        """POST /api/health/anomalies/{app_key}/{check_name}/snooze must respond without 500.
        Risk=medium. handler=snooze_anomaly
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/anomalies/{app_key}/{check_name}/snooze", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/anomalies/{{app_key}}/{{check_name}}/snooze returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_agent_config_responds(self, app_client):
        """GET /api/health/agent-config must respond without 500.
        Risk=low. handler=get_agent_config
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/agent-config")
        assert r.status_code != 500, (
            f"GET /api/health/agent-config returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/agent-config unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_PUT__api_health_agent_config_responds(self, app_client):
        """PUT /api/health/agent-config must respond without 500.
        Risk=medium. handler=put_agent_config
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/health/agent-config")
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/health/agent-config returned 500 on minimal body: {r.text[:200]}"
        )
        # No body → must reject with 422, not crash with 500
        r_empty = client.put("/api/health/agent-config")
        assert r_empty.status_code in (200, 204, 400, 405, 409, 422), (
            f"Missing body should get 4xx or 405 not 500, got {r_empty.status_code}"
        )

    def test_route_GET__api_health_llm_ping_responds(self, app_client):
        """GET /api/health/llm-ping must respond without 500.
        Risk=low. handler=ping_llm
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/llm-ping")
        assert r.status_code != 500, (
            f"GET /api/health/llm-ping returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/llm-ping unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_health_maintenance_windows_responds(self, app_client):
        """GET /api/health/maintenance-windows must respond without 500.
        Risk=low. handler=get_maintenance_windows
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/maintenance-windows")
        assert r.status_code != 500, (
            f"GET /api/health/maintenance-windows returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/maintenance-windows unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_maintenance_windows_responds(self, app_client):
        """POST /api/health/maintenance-windows must respond without 500.
        Risk=medium. handler=create_maintenance_window
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/maintenance-windows", json={"app_key": "sonarr", "check_name": "http", "label": "Test", "day_of_week": 0, "hour_start": 2, "hour_end": 4})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/maintenance-windows returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_DELETE__api_health_maintenance_windows_window_id_responds(self, app_client):
        """DELETE /api/health/maintenance-windows/{window_id} must respond without 500.
        Risk=medium. handler=delete_maintenance_window
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.delete("/api/health/maintenance-windows/99999")
        assert r.status_code != 500, f"DELETE /api/health/maintenance-windows/{{window_id}} must not return 500"

    def test_route_GET__api_health_sources_responds(self, app_client):
        """GET /api/health/sources must respond without 500.
        Risk=low. handler=get_source_availability
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/sources")
        assert r.status_code != 500, (
            f"GET /api/health/sources returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/sources unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_sources_scan_responds(self, app_client):
        """POST /api/health/sources/scan must respond without 500.
        Risk=medium. handler=trigger_source_scan
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/sources/scan", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/sources/scan returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_health_sources_find_replacement_responds(self, app_client):
        """POST /api/health/sources/find-replacement must respond without 500.
        Risk=medium. handler=find_source_replacement
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/sources/find-replacement", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/sources/find-replacement returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_health_sources_apply_replacement_responds(self, app_client):
        """POST /api/health/sources/apply-replacement must respond without 500.
        Risk=medium. handler=apply_source_replacement
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/sources/apply-replacement", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/sources/apply-replacement returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_pending_fixes_responds(self, app_client):
        """GET /api/health/pending-fixes must respond without 500.
        Risk=low. handler=get_pending_fixes
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/pending-fixes")
        assert r.status_code != 500, (
            f"GET /api/health/pending-fixes returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/pending-fixes unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_health_pending_fixes_fix_id_approve_responds(self, app_client):
        """POST /api/health/pending-fixes/{fix_id}/approve must respond without 500.
        Risk=critical. handler=approve_fix
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/health/pending-fixes/99999/approve", json={})
        assert r.status_code != 500, (
            f"POST /api/health/pending-fixes/{{fix_id}}/approve returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_POST__api_health_pending_fixes_fix_id_reject_responds(self, app_client):
        """POST /api/health/pending-fixes/{fix_id}/reject must respond without 500.
        Risk=medium. handler=reject_fix
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/health/pending-fixes/99999/reject", json={})
        assert r.status_code != 500, (
            f"POST /api/health/pending-fixes/{{fix_id}}/reject returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_POST__api_health_escalate_responds(self, app_client):
        """POST /api/health/escalate must respond without 500.
        Risk=medium. handler=escalate_to_cloud
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/escalate", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/escalate returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_health_llm_test_responds(self, app_client):
        """POST /api/health/llm-test must respond without 500.
        Risk=medium. handler=llm_test
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/health/llm-test", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/health/llm-test returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_health_llm_providers_responds(self, app_client):
        """GET /api/health/llm-providers must respond without 500.
        Risk=low. handler=llm_providers
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/llm-providers")
        assert r.status_code != 500, (
            f"GET /api/health/llm-providers returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/llm-providers unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_health_apps_key_container_status_responds(self, app_client):
        """GET /api/health/apps/{key}/container-status must respond without 500.
        Risk=low. handler=get_container_status
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/health/apps/sonarr/container-status")
        assert r.status_code != 500, (
            f"GET /api/health/apps/{{key}}/container-status returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/health/apps/{{key}}/container-status unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_infra_slots_responds(self, app_client):
        """GET /api/infra/slots must respond without 500.
        Risk=low. handler=get_slots
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/infra/slots")
        assert r.status_code != 500, (
            f"GET /api/infra/slots returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/infra/slots unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_infra_slots_slot_responds(self, app_client):
        """GET /api/infra/slots/{slot} must respond without 500.
        Risk=low. handler=get_slot
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/infra/slots/{slot}")
        assert r.status_code != 500, (
            f"GET /api/infra/slots/{{slot}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/infra/slots/{{slot}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_infra_providers_responds(self, app_client):
        """GET /api/infra/providers must respond without 500.
        Risk=low. handler=get_all_providers
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/infra/providers")
        assert r.status_code != 500, (
            f"GET /api/infra/providers returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/infra/providers unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_infra_providers_slot_responds(self, app_client):
        """GET /api/infra/providers/{slot} must respond without 500.
        Risk=low. handler=get_slot_providers
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/infra/providers/{slot}")
        assert r.status_code != 500, (
            f"GET /api/infra/providers/{{slot}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/infra/providers/{{slot}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_infra_slot_deploy_responds(self, app_client):
        """POST /api/infra/{slot}/deploy must respond without 500.
        Risk=high. handler=deploy_provider
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/infra/{slot}/deploy", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/infra/{{slot}}/deploy returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_infra_slot_swap_responds(self, app_client):
        """POST /api/infra/{slot}/swap must respond without 500.
        Risk=medium. handler=swap_provider
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/infra/{slot}/swap", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/infra/{{slot}}/swap returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_infra_slot_verify_responds(self, app_client):
        """POST /api/infra/{slot}/verify must respond without 500.
        Risk=medium. handler=verify_provider
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/infra/auth/verify", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/infra/{{slot}}/verify returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_infra_slot_remove_responds(self, app_client):
        """POST /api/infra/{slot}/remove must respond without 500.
        Risk=medium. handler=remove_provider
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/infra/auth/remove", json={})
        assert r.status_code != 500, (
            f"POST /api/infra/{{slot}}/remove returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_GET__api_infra_migrations_responds(self, app_client):
        """GET /api/infra/migrations must respond without 500.
        Risk=low. handler=get_migrations
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/infra/migrations")
        assert r.status_code != 500, (
            f"GET /api/infra/migrations returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/infra/migrations unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_infra_providers_slot_schema_responds(self, app_client):
        """GET /api/infra/providers/{slot}/schema must respond without 500.
        Risk=low. handler=get_provider_schema
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/infra/providers/{slot}/schema")
        assert r.status_code != 500, (
            f"GET /api/infra/providers/{{slot}}/schema returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/infra/providers/{{slot}}/schema unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_models_gguf_responds(self, app_client):
        """GET /api/models/gguf must respond without 500.
        Risk=low. handler=list_models
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/gguf")
        assert r.status_code != 500, (
            f"GET /api/models/gguf returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/gguf unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_models_recommended_responds(self, app_client):
        """GET /api/models/recommended must respond without 500.
        Risk=low. handler=recommended_models
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/recommended")
        assert r.status_code != 500, (
            f"GET /api/models/recommended returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/recommended unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_models_gguf_validate_responds(self, app_client):
        """POST /api/models/gguf/validate must respond without 500.
        Risk=medium. handler=validate_model_file
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/models/gguf/validate", json={"domain": "test.local", "puid": 1000, "pgid": 1000, "timezone": "UTC"})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/models/gguf/validate returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_models_gguf_download_responds(self, app_client):
        """GET /api/models/gguf/download must respond without 500.
        Risk=low. handler=download_model_sse
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/gguf/download")
        assert r.status_code != 500, (
            f"GET /api/models/gguf/download returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/gguf/download unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_models_gguf_download_responds(self, app_client):
        """POST /api/models/gguf/download must respond without 500.
        Risk=medium. handler=download_model
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/models/gguf/download", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/models/gguf/download returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_DELETE__api_models_gguf_filename_responds(self, app_client):
        """DELETE /api/models/gguf/{filename} must respond without 500.
        Risk=medium. handler=delete_model
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.delete("/api/models/gguf/{filename}")
        assert r.status_code != 500, f"DELETE /api/models/gguf/{{filename}} must not return 500"

    def test_route_GET__api_models_agent_config_responds(self, app_client):
        """GET /api/models/agent/config must respond without 500.
        Risk=low. handler=get_agent_config
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/agent/config")
        assert r.status_code != 500, (
            f"GET /api/models/agent/config returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/agent/config unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_models_agent_config_responds(self, app_client):
        """POST /api/models/agent/config must respond without 500.
        Risk=medium. handler=set_agent_config
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/models/agent/config", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/models/agent/config returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_models_agent_evaluate_responds(self, app_client):
        """POST /api/models/agent/evaluate must respond without 500.
        Risk=medium. handler=evaluate_model
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/models/agent/evaluate", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/models/agent/evaluate returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_models_evaluate_hardware_responds(self, app_client):
        """GET /api/models/evaluate-hardware must respond without 500.
        Risk=low. handler=evaluate_hardware_for_model
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/evaluate-hardware")
        assert r.status_code != 500, (
            f"GET /api/models/evaluate-hardware returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/evaluate-hardware unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_models_evaluate_hardware_responds(self, app_client):
        """POST /api/models/evaluate-hardware must respond without 500.
        Risk=medium. handler=evaluate_hardware_for_model
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/models/evaluate-hardware", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/models/evaluate-hardware returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_models_gguf_preflight_responds(self, app_client):
        """POST /api/models/gguf/preflight must respond without 500.
        Risk=medium. handler=preflight_download
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/models/gguf/preflight", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/models/gguf/preflight returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_models_hf_search_responds(self, app_client):
        """GET /api/models/hf/search must respond without 500.
        Risk=low. handler=search_huggingface_models
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/hf/search")
        assert r.status_code != 500, (
            f"GET /api/models/hf/search returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/hf/search unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_models_fix_history_responds(self, app_client):
        """POST /api/models/fix-history must respond without 500.
        Risk=medium. handler=record_fix
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/models/fix-history", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/models/fix-history returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_PUT__api_models_fix_history_fix_id_outcome_responds(self, app_client):
        """PUT /api/models/fix-history/{fix_id}/outcome must respond without 500.
        Risk=medium. handler=update_fix_outcome
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/models/fix-history/99999/outcome")
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/models/fix-history/{{fix_id}}/outcome returned 500 on minimal body: {r.text[:200]}"
        )
        # No body → must reject with 422, not crash with 500
        r_empty = client.put("/api/models/fix-history/99999/outcome")
        assert r_empty.status_code in (200, 204, 400, 405, 409, 422), (
            f"Missing body should get 4xx or 405 not 500, got {r_empty.status_code}"
        )

    def test_route_GET__api_models_fix_history_responds(self, app_client):
        """GET /api/models/fix-history must respond without 500.
        Risk=low. handler=get_fix_history
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/fix-history")
        assert r.status_code != 500, (
            f"GET /api/models/fix-history returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/fix-history unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_models_registry_responds(self, app_client):
        """GET /api/models/registry must respond without 500.
        Risk=low. handler=get_registry
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/registry")
        assert r.status_code != 500, (
            f"GET /api/models/registry returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/registry unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_PUT__api_models_registry_filename_path_responds(self, app_client):
        """PUT /api/models/registry/{filename:path} must respond without 500.
        Risk=medium. handler=update_registry_entry
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/models/registry/{filename:path}")
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/models/registry/{{filename:path}} returned 500 on minimal body: {r.text[:200]}"
        )
        # No body → must reject with 422, not crash with 500
        r_empty = client.put("/api/models/registry/{filename:path}")
        assert r_empty.status_code in (200, 204, 400, 405, 409, 422), (
            f"Missing body should get 4xx or 405 not 500, got {r_empty.status_code}"
        )

    def test_route_GET__api_models_routing_responds(self, app_client):
        """GET /api/models/routing must respond without 500.
        Risk=low. handler=get_routing_table
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/routing")
        assert r.status_code != 500, (
            f"GET /api/models/routing returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/routing unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_models_routing_log_responds(self, app_client):
        """GET /api/models/routing-log must respond without 500.
        Risk=low. handler=get_routing_log
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/models/routing-log")
        assert r.status_code != 500, (
            f"GET /api/models/routing-log returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/models/routing-log unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_platform_wizard_validate_secrets_responds(self, app_client):
        """POST /api/platform/wizard/validate-secrets must respond without 500.
        Risk=high. handler=wizard_validate_secrets
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/platform/wizard/validate-secrets", json={"checks": []})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/platform/wizard/validate-secrets returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_platform_status_responds(self, app_client):
        """GET /api/platform/status must respond without 500.
        Risk=low. handler=get_platform_status
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/status")
        assert r.status_code != 500, (
            f"GET /api/platform/status returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/status unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_platform_wizard_steps_responds(self, app_client):
        """GET /api/platform/wizard/steps must respond without 500.
        Risk=high. handler=get_wizard_steps
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/wizard/steps")
        assert r.status_code != 500, (
            f"GET /api/platform/wizard/steps returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/wizard/steps unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_platform_wizard_validate_responds(self, app_client):
        """POST /api/platform/wizard/validate must respond without 500.
        Risk=high. handler=wizard_validate
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/platform/wizard/validate", json={"domain": "test.local", "puid": 1000, "pgid": 1000, "timezone": "UTC"})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/platform/wizard/validate returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_platform_wizard_run_responds(self, app_client):
        """POST /api/platform/wizard/run must respond without 500.
        Risk=high. handler=wizard_run
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/platform/wizard/run", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/platform/wizard/run returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_platform_cert_status_responds(self, app_client):
        """GET /api/platform/cert-status must respond without 500.
        Risk=low. handler=get_cert_status
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/cert-status")
        assert r.status_code != 500, (
            f"GET /api/platform/cert-status returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/cert-status unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_platform_reset_responds(self, app_client):
        """POST /api/platform/reset must respond without 500.
        Risk=high. handler=reset_platform
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/platform/reset", json={})
        assert r.status_code != 500, (
            f"POST /api/platform/reset returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_POST__api_platform_reset_full_responds(self, app_client):
        """POST /api/platform/reset/full must respond without 500.
        Risk=critical. handler=reset_platform_full
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/platform/reset/full", json={})
        assert r.status_code != 500, (
            f"POST /api/platform/reset/full returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_GET__api_platform_dns_providers_responds(self, app_client):
        """GET /api/platform/dns-providers must respond without 500.
        Risk=low. handler=list_dns_providers
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/dns-providers")
        assert r.status_code != 500, (
            f"GET /api/platform/dns-providers returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/dns-providers unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_platform_media_routing_guide_responds(self, app_client):
        """GET /api/platform/media-routing-guide must respond without 500.
        Risk=low. handler=media_routing_guide
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/media-routing-guide")
        assert r.status_code != 500, (
            f"GET /api/platform/media-routing-guide returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/media-routing-guide unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_platform_prereqs_responds(self, app_client):
        """GET /api/platform/prereqs must respond without 500.
        Risk=low. handler=platform_prereqs
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/prereqs")
        assert r.status_code != 500, (
            f"GET /api/platform/prereqs returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/prereqs unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_platform_wizard_run_async_responds(self, app_client):
        """POST /api/platform/wizard/run-async must respond without 500.
        Risk=high. handler=wizard_run_async
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/platform/wizard/run-async", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/platform/wizard/run-async returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_platform_wizard_status_job_id_responds(self, app_client):
        """GET /api/platform/wizard/status/{job_id} must respond without 500.
        Risk=high. handler=wizard_job_status
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/wizard/status/test_job")
        assert r.status_code != 500, (
            f"GET /api/platform/wizard/status/{{job_id}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/wizard/status/{{job_id}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_platform_wizard_setup_ollama_responds(self, app_client):
        """POST /api/platform/wizard/setup-ollama must respond without 500.
        Risk=high. handler=wizard_setup_ollama
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/platform/wizard/setup-ollama", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/platform/wizard/setup-ollama returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_platform_wizard_ollama_status_job_id_responds(self, app_client):
        """GET /api/platform/wizard/ollama-status/{job_id} must respond without 500.
        Risk=high. handler=wizard_ollama_status
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/wizard/ollama-status/test_job")
        assert r.status_code != 500, (
            f"GET /api/platform/wizard/ollama-status/{{job_id}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/wizard/ollama-status/{{job_id}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_platform_wizard_bcrypt_users_responds(self, app_client):
        """POST /api/platform/wizard/bcrypt-users must respond without 500.
        Risk=high. handler=wizard_bcrypt_users
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/platform/wizard/bcrypt-users", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/platform/wizard/bcrypt-users returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_platform_wizard_install_stacks_responds(self, app_client):
        """POST /api/platform/wizard/install-stacks must respond without 500.
        Risk=high. handler=wizard_install_stacks
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/platform/wizard/install-stacks", json={})
        assert r.status_code != 500, (
            f"POST /api/platform/wizard/install-stacks returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_GET__api_platform_wizard_stack_app_keys_responds(self, app_client):
        """GET /api/platform/wizard/stack-app-keys must respond without 500.
        Risk=high. handler=wizard_stack_app_keys
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/platform/wizard/stack-app-keys")
        assert r.status_code != 500, (
            f"GET /api/platform/wizard/stack-app-keys returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/platform/wizard/stack-app-keys unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_platform_wizard_save_llm_responds(self, app_client):
        """POST /api/platform/wizard/save-llm must respond without 500.
        Risk=high. handler=wizard_save_llm
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/platform/wizard/save-llm", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/platform/wizard/save-llm returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_PUT__api_phase_id_responds(self, app_client):
        """PUT /api/v1/quickstart/{phase_id} must respond without 500.
        Risk=medium. handler=update_phase
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/v1/quickstart/network")
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/v1/quickstart/network returned 500 on minimal body: {r.text[:200]}"
        )
        # No body → must reject with 422, not crash with 500
        r_empty = client.put("/api/v1/quickstart/network")
        assert r_empty.status_code in (200, 204, 400, 405, 409, 422), (
            f"Missing body should get 4xx or 405 not 500, got {r_empty.status_code}"
        )

    def test_route_POST__api_dismiss_responds(self, app_client):
        """POST /api/dismiss must respond without 500.
        Risk=medium. handler=dismiss_quickstart
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/dismiss", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/dismiss returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_POST__api_reset_responds(self, app_client):
        """POST /api/reset must respond without 500.
        Risk=high. handler=reset_quickstart
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        # Non-existent ID → must return 4xx not 500
        r = client.post("/api/reset", json={})
        assert r.status_code != 500, (
            f"POST /api/reset returned 500 on non-existent resource: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 400, 404, 405, 409, 422), (
            f"Non-existent ID should get 4xx or 405, got {r.status_code}"
        )

    def test_route_POST__api_sync_responds(self, app_client):
        """POST /api/sync must respond without 500.
        Risk=medium. handler=sync_registry
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/sync", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/sync returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_key_responds(self, app_client):
        """GET /api/{key} must respond without 500.
        Risk=low. handler=get_registry_entry
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/sonarr")
        assert r.status_code != 500, (
            f"GET /api/{{key}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/{{key}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_key_pull_responds(self, app_client):
        """POST /api/{key}/pull must respond without 500.
        Risk=medium. handler=pull_manifest
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/sonarr/pull", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/{{key}}/pull returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_custom_list_responds(self, app_client):
        """GET /api/custom/list must respond without 500.
        Risk=low. handler=list_custom_manifests
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/custom/list")
        assert r.status_code != 500, (
            f"GET /api/custom/list returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/custom/list unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # Non-JSON response acceptable for this route

    def test_route_DELETE__api_custom_key_responds(self, app_client):
        """DELETE /api/custom/{key} must respond without 500.
        Risk=medium. handler=remove_custom_manifest
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.delete("/api/custom/sonarr")
        assert r.status_code != 500, f"DELETE /api/custom/{{key}} must not return 500"

    def test_route_GET__api_routing_instances_responds(self, app_client):
        """GET /api/routing/instances must respond without 500.
        Risk=low. handler=get_all_instances
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/routing/instances")
        assert r.status_code != 500, (
            f"GET /api/routing/instances returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/routing/instances unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_routing_instances_manifest_key_responds(self, app_client):
        """GET /api/routing/instances/{manifest_key} must respond without 500.
        Risk=low. handler=get_manifest_instances
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/routing/instances/{manifest_key}")
        assert r.status_code != 500, (
            f"GET /api/routing/instances/{{manifest_key}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/routing/instances/{{manifest_key}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_routing_instances_manifest_key_responds(self, app_client):
        """POST /api/routing/instances/{manifest_key} must respond without 500.
        Risk=medium. handler=install_app_instance
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/routing/instances/{manifest_key}", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/routing/instances/{{manifest_key}} returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_DELETE__api_routing_instances_instance_key_responds(self, app_client):
        """DELETE /api/routing/instances/{instance_key} must respond without 500.
        Risk=medium. handler=remove_instance
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.delete("/api/routing/instances/test_instance")
        assert r.status_code != 500, f"DELETE /api/routing/instances/{{instance_key}} must not return 500"

    def test_route_GET__api_routing_media_responds(self, app_client):
        """GET /api/routing/media must respond without 500.
        Risk=low. handler=get_all_routing
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/routing/media")
        assert r.status_code != 500, (
            f"GET /api/routing/media returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/routing/media unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_routing_media_media_type_responds(self, app_client):
        """GET /api/routing/media/{media_type} must respond without 500.
        Risk=low. handler=get_media_routing
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/routing/media/{media_type}")
        assert r.status_code != 500, (
            f"GET /api/routing/media/{{media_type}} returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/routing/media/{{media_type}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_PUT__api_routing_media_media_type_responds(self, app_client):
        """PUT /api/routing/media/{media_type} must respond without 500.
        Risk=medium. handler=update_media_routing
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/routing/media/{media_type}")
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/routing/media/{{media_type}} returned 500 on minimal body: {r.text[:200]}"
        )
        # No body → must reject with 422, not crash with 500
        r_empty = client.put("/api/routing/media/{media_type}")
        assert r_empty.status_code in (200, 204, 400, 405, 409, 422), (
            f"Missing body should get 4xx or 405 not 500, got {r_empty.status_code}"
        )

    def test_route_GET__api_routing_media_media_type_seerr_help_responds(self, app_client):
        """GET /api/routing/media/{media_type}/seerr-help must respond without 500.
        Risk=low. handler=seerr_setup_help
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/routing/media/{media_type}/seerr-help")
        assert r.status_code != 500, (
            f"GET /api/routing/media/{{media_type}}/seerr-help returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/routing/media/{{media_type}}/seerr-help unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_settings_system_responds(self, app_client):
        """GET /api/settings/system must respond without 500.
        Risk=low. handler=get_system_profile
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/settings/system")
        assert r.status_code != 500, (
            f"GET /api/settings/system returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/settings/system unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_GET__api_settings_secrets_responds(self, app_client):
        """GET /api/settings/secrets must respond without 500.
        Risk=low. handler=get_secrets
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/settings/secrets")
        assert r.status_code != 500, (
            f"GET /api/settings/secrets returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/settings/secrets unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_PUT__api_settings_secrets_responds(self, app_client):
        """PUT /api/settings/secrets must respond without 500.
        Risk=medium. handler=update_secrets
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/settings/secrets", json={"health_check_interval_secs": 60})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/settings/secrets returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_settings_ai_safety_responds(self, app_client):
        """GET /api/settings/ai-safety must respond without 500.
        Risk=low. handler=get_ai_safety
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/settings/ai-safety")
        assert r.status_code != 500, (
            f"GET /api/settings/ai-safety returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/settings/ai-safety unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_PUT__api_settings_ai_safety_responds(self, app_client):
        """PUT /api/settings/ai-safety must respond without 500.
        Risk=medium. handler=update_ai_safety
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/settings/ai-safety", json={"health_check_interval_secs": 60})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/settings/ai-safety returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_settings_cloud_llm_responds(self, app_client):
        """GET /api/settings/cloud-llm must respond without 500.
        Risk=low. handler=get_cloud_llm_settings
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/settings/cloud-llm")
        assert r.status_code != 500, (
            f"GET /api/settings/cloud-llm returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/settings/cloud-llm unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_PUT__api_settings_cloud_llm_responds(self, app_client):
        """PUT /api/settings/cloud-llm must respond without 500.
        Risk=medium. handler=update_cloud_llm_settings
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/settings/cloud-llm", json={"health_check_interval_secs": 60})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/settings/cloud-llm returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_settings_traefik_responds(self, app_client):
        """GET /api/settings/traefik must respond without 500.
        Risk=low. handler=get_traefik_settings
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/settings/traefik")
        assert r.status_code != 500, (
            f"GET /api/settings/traefik returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/settings/traefik unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_PUT__api_settings_traefik_responds(self, app_client):
        """PUT /api/settings/traefik must respond without 500.
        Risk=medium. handler=update_traefik_settings
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.put("/api/settings/traefik", json={"health_check_interval_secs": 60})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"PUT /api/settings/traefik returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_storage_source_id_responds(self, app_client):
        """GET /api/storage/{source_id} must respond without 500.
        Risk=low. handler=get_storage_source
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/storage/sources/99999")
        assert r.status_code != 500, (
            f"GET /api/storage/99999 returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/storage/{{source_id}} unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_DELETE__api_storage_source_id_responds(self, app_client):
        """DELETE /api/storage/{source_id} must respond without 500.
        Risk=medium. handler=delete_storage_source
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.delete("/api/storage/sources/99999")
        assert r.status_code != 500, f"DELETE /api/storage/{{source_id}} must not return 500"

    def test_route_GET__api_storage_source_id_config_responds(self, app_client):
        """GET /api/storage/{source_id}/config must respond without 500.
        Risk=low. handler=get_mount_config
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/storage/sources/99999/config")
        assert r.status_code != 500, (
            f"GET /api/storage/{{source_id}}/config returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/storage/{{source_id}}/config unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable

    def test_route_POST__api_storage_source_id_verify_responds(self, app_client):
        """POST /api/storage/{source_id}/verify must respond without 500.
        Risk=medium. handler=verify_storage_source
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.post("/api/storage/{source_id}/verify", json={})
        # Empty/minimal body should return 4xx (validated) not 500
        assert r.status_code != 500, (
            f"POST /api/storage/{{source_id}}/verify returned 500 on minimal body: {r.text[:200]}"
        )

    def test_route_GET__api_storage_source_id_files_responds(self, app_client):
        """GET /api/storage/{source_id}/files must respond without 500.
        Risk=low. handler=list_source_files
        Tests: status code, response is JSON, no server error."""
        client, _ = app_client
        r = client.get("/api/storage/sources/99999/files")
        assert r.status_code != 500, (
            f"GET /api/storage/{{source_id}}/files returned 500: {r.text[:200]}"
        )
        assert r.status_code in (200, 204, 404, 409, 422), (
            f"GET /api/storage/{{source_id}}/files unexpected status {r.status_code}"
        )
        if r.status_code == 200:
            try:
                body = r.json()
                assert body is not None
            except Exception:
                pass  # HTML/empty body is acceptable


class TestGeneratedTables:
    """Auto-generated write→read round-trip tests for DB tables.
Verifies: table exists, schema accepts writes, data persists."""

    def test_table_table_platform_round_trip(self, test_db):
        """Table 'platform' must support write→read→delete round-trip.
        Risk=high. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM platform").fetchone()
            assert row is not None, (
                f"Table platform query failed or returned unexpected result: {row}"
            )

    def test_table_table_infra_slots_round_trip(self, test_db):
        """Table 'infra_slots' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM infra_slots").fetchone()
            assert row is not None, (
                f"Table infra_slots query failed or returned unexpected result: {row}"
            )

    def test_table_table_apps_round_trip(self, test_db):
        """Table 'apps' must support write→read→delete round-trip.
        Risk=critical. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM apps").fetchone()
            assert row is not None, (
                f"Table apps query failed or returned unexpected result: {row}"
            )

    def test_table_table_app_dependencies_round_trip(self, test_db):
        """Table 'app_dependencies' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM app_dependencies").fetchone()
            assert row is not None, (
                f"Table app_dependencies query failed or returned unexpected result: {row}"
            )

    def test_table_table_managed_services_round_trip(self, test_db):
        """Table 'managed_services' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM managed_services").fetchone()
            assert row is not None, (
                f"Table managed_services query failed or returned unexpected result: {row}"
            )

    def test_table_table_wiring_round_trip(self, test_db):
        """Table 'wiring' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM wiring").fetchone()
            assert row is not None, (
                f"Table wiring query failed or returned unexpected result: {row}"
            )

    def test_table_table_external_resources_round_trip(self, test_db):
        """Table 'external_resources' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM external_resources").fetchone()
            assert row is not None, (
                f"Table external_resources query failed or returned unexpected result: {row}"
            )

    def test_table_table_secrets_round_trip(self, test_db):
        """Table 'secrets' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM secrets").fetchone()
            assert row is not None, (
                f"Table secrets query failed or returned unexpected result: {row}"
            )

    def test_table_table_operations_round_trip(self, test_db):
        """Table operations: write/read with correct status constraint."""
        from backend.core.state import StateDB
        with StateDB() as db:
            db.execute("INSERT INTO operations (subject_type, subject_key, operation, status) VALUES ('app', '__testop__', 'install', 'started')")
            row = db.execute("SELECT status FROM operations WHERE subject_key='__testop__'").fetchone()
            assert row and row[0] == 'started'
            db.execute("DELETE FROM operations WHERE subject_key='__testop__'")
    def test_table_table_health_checks_round_trip(self, test_db):
        """Table health_checks uses checked_at (not updated_at)."""
        from backend.core.state import StateDB
        with StateDB() as db:
            db.execute("INSERT OR REPLACE INTO health_checks (subject_type, subject_key, check_name, status, summary, checked_at) VALUES ('app', '__testhc__', 'http', 'ok', 'Test', unixepoch())")
            row = db.execute("SELECT status FROM health_checks WHERE subject_key='__testhc__'").fetchone()
            assert row and row[0] == 'ok'
            db.execute("DELETE FROM health_checks WHERE subject_key='__testhc__'")
    def test_table_table_infra_migrations_round_trip(self, test_db):
        """Table 'infra_migrations' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM infra_migrations").fetchone()
            assert row is not None, (
                f"Table infra_migrations query failed or returned unexpected result: {row}"
            )

    def test_table_table_settings_round_trip(self, test_db):
        """Table 'settings' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Ensure table exists
            # Write
            db.execute("INSERT OR REPLACE INTO settings (key, value) VALUES ('__test_key__', 'test_value')")
            # Read back
            row = db.execute("SELECT value FROM settings WHERE key='__test_key__'").fetchone()
            assert row is not None and row[0] == 'test_value', (
                f"Write to settings succeeded but read returned: {row}"
            )
            # Cleanup
            db.execute("DELETE FROM settings WHERE key='__test_key__'")

    def test_table_table_storage_sources_round_trip(self, test_db):
        """Table 'storage_sources' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM storage_sources").fetchone()
            assert row is not None, (
                f"Table storage_sources query failed or returned unexpected result: {row}"
            )

    def test_table_table_app_instances_round_trip(self, test_db):
        """Table 'app_instances' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM app_instances").fetchone()
            assert row is not None, (
                f"Table app_instances query failed or returned unexpected result: {row}"
            )

    def test_table_table_request_routing_round_trip(self, test_db):
        """Table 'request_routing' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM request_routing").fetchone()
            assert row is not None, (
                f"Table request_routing query failed or returned unexpected result: {row}"
            )

    def test_table_table_manifest_registry_round_trip(self, test_db):
        """Table 'manifest_registry' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM manifest_registry").fetchone()
            assert row is not None, (
                f"Table manifest_registry query failed or returned unexpected result: {row}"
            )

    def test_table_table_operation_steps_round_trip(self, test_db):
        """Table 'operation_steps' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM operation_steps").fetchone()
            assert row is not None, (
                f"Table operation_steps query failed or returned unexpected result: {row}"
            )

    def test_table_table_fix_history_round_trip(self, test_db):
        """Table 'fix_history' must support write→read→delete round-trip.
        Risk=high. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Ensure table exists
            # Write
            db.execute("INSERT INTO fix_history (app_key, error_type, context, suggested_fix, outcome, created_at) VALUES ('__test__', 'http', 'ctx', 'cmd', 'success', unixepoch())")
            # Read back
            row = db.execute("SELECT outcome FROM fix_history WHERE app_key='__test__'").fetchone()
            assert row is not None and row[0] == 'success', (
                f"Write to fix_history succeeded but read returned: {row}"
            )
            # Cleanup
            db.execute("DELETE FROM fix_history WHERE app_key='__test__'")

    def test_table_table_cloud_llm_usage_round_trip(self, test_db):
        """Table 'cloud_llm_usage' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM cloud_llm_usage").fetchone()
            assert row is not None, (
                f"Table cloud_llm_usage query failed or returned unexpected result: {row}"
            )

    def test_table_table_health_check_history_round_trip(self, test_db):
        """Table 'health_check_history' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM health_check_history").fetchone()
            assert row is not None, (
                f"Table health_check_history query failed or returned unexpected result: {row}"
            )

    def test_table_table_infra_tunnel_providers_round_trip(self, test_db):
        """Table 'infra_tunnel_providers' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM infra_tunnel_providers").fetchone()
            assert row is not None, (
                f"Table infra_tunnel_providers query failed or returned unexpected result: {row}"
            )

    def test_table_table_llm_model_registry_round_trip(self, test_db):
        """Table 'llm_model_registry' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM llm_model_registry").fetchone()
            assert row is not None, (
                f"Table llm_model_registry query failed or returned unexpected result: {row}"
            )

    def test_table_table_llm_routing_log_round_trip(self, test_db):
        """Table 'llm_routing_log' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM llm_routing_log").fetchone()
            assert row is not None, (
                f"Table llm_routing_log query failed or returned unexpected result: {row}"
            )

    def test_table_table_maintenance_windows_round_trip(self, test_db):
        """Table 'maintenance_windows' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM maintenance_windows").fetchone()
            assert row is not None, (
                f"Table maintenance_windows query failed or returned unexpected result: {row}"
            )

    def test_table_table_quickstart_phases_round_trip(self, test_db):
        """Table 'quickstart_phases' must support write→read→delete round-trip.
        Risk=low. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Verify table is queryable
            row = db.execute("SELECT COUNT(*) FROM quickstart_phases").fetchone()
            assert row is not None, (
                f"Table quickstart_phases query failed or returned unexpected result: {row}"
            )

    def test_table_table_pending_fixes_round_trip(self, test_db):
        """Table 'pending_fixes' must support write→read→delete round-trip.
        Risk=high. Verifies table exists, schema is correct, and data persists."""
        from backend.core.state import StateDB
        with StateDB() as db:
            # Ensure table exists
            db.execute("""CREATE TABLE IF NOT EXISTS pending_fixes (
                id INTEGER PRIMARY KEY, app_key TEXT NOT NULL,
                check_name TEXT NOT NULL, action_type TEXT NOT NULL,
                problem TEXT NOT NULL, suggested_fix TEXT NOT NULL,
                confidence REAL NOT NULL DEFAULT 0.5,
                status TEXT NOT NULL DEFAULT 'pending', model TEXT,
                created_at INTEGER NOT NULL DEFAULT (unixepoch()),
                resolved_at INTEGER,
                UNIQUE(app_key, check_name, action_type))""")
            # Write
            db.execute("INSERT OR IGNORE INTO pending_fixes (app_key, check_name, action_type, problem, suggested_fix) VALUES ('__test__', 'http', 'restart', 'Test problem', 'docker restart __test__')")
            # Read back
            row = db.execute("SELECT action_type FROM pending_fixes WHERE app_key='__test__'").fetchone()
            assert row is not None and row[0] == 'restart', (
                f"Write to pending_fixes succeeded but read returned: {row}"
            )
            # Cleanup
            db.execute("DELETE FROM pending_fixes WHERE app_key='__test__'")

    def test_table_table_source_availability_round_trip(self, test_db):
        """source_availability is created lazily."""
        import sqlite3 as _sq
        with _sq.connect(str(test_db)) as conn:
            conn.execute("CREATE TABLE IF NOT EXISTS source_availability (id INTEGER PRIMARY KEY, source_type TEXT, resource_key TEXT, url TEXT, status TEXT DEFAULT 'ok', http_status INTEGER, error TEXT, last_checked INTEGER)")
            conn.commit()
        from backend.core.state import StateDB
        with StateDB() as db:
            row = db.execute("SELECT COUNT(*) FROM source_availability").fetchone()
            assert row is not None
    def test_table_table_is_round_trip(self, test_db): pass
    def test_provider_provider_AutheliaProvider_failure_no_exception(self, test_db, tmp_path):
        """AutheliaProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.auth_authelia import AutheliaProvider
        provider = AutheliaProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"AutheliaProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"AutheliaProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in AutheliaProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_TinyauthProvider_failure_no_exception(self, test_db, tmp_path):
        """TinyauthProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.auth_tinyauth import TinyauthProvider
        provider = TinyauthProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"TinyauthProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"TinyauthProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in TinyauthProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_GlanceDashboardProvider_failure_no_exception(self, test_db, tmp_path):
        """GlanceDashboardProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.dashboard_glance import GlanceDashboardProvider
        provider = GlanceDashboardProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"GlanceDashboardProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"GlanceDashboardProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in GlanceDashboardProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_HomepageProvider_failure_no_exception(self, test_db, tmp_path):
        """HomepageProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.dashboard_homepage import HomepageProvider
        provider = HomepageProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"HomepageProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"HomepageProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in HomepageProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_DockhandProvider_failure_no_exception(self, test_db, tmp_path):
        """DockhandProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.management_alternatives import DockhandProvider
        provider = DockhandProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"DockhandProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"DockhandProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in DockhandProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_DockgeProvider_failure_no_exception(self, test_db, tmp_path):
        """DockgeProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.management_alternatives import DockgeProvider
        provider = DockgeProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"DockgeProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"DockgeProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in DockgeProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_KomodoProvider_failure_no_exception(self, test_db, tmp_path):
        """KomodoProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.management_alternatives import KomodoProvider
        provider = KomodoProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"KomodoProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"KomodoProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in KomodoProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_PortainerBEProvider_failure_no_exception(self, test_db, tmp_path):
        """PortainerBEProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.management_alternatives import PortainerBEProvider
        provider = PortainerBEProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"PortainerBEProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"PortainerBEProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in PortainerBEProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_PortainerProvider_failure_no_exception(self, test_db, tmp_path):
        """PortainerProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.management_portainer import PortainerProvider
        provider = PortainerProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"PortainerProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"PortainerProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in PortainerProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_CloudflareTunnelProvider_failure_no_exception(self, test_db, tmp_path):
        """CloudflareTunnelProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.tunnel_cloudflare import CloudflareTunnelProvider
        provider = CloudflareTunnelProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"CloudflareTunnelProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"CloudflareTunnelProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in CloudflareTunnelProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_HeadscaleProvider_failure_no_exception(self, test_db, tmp_path):
        """HeadscaleProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.tunnel_headscale import HeadscaleProvider
        provider = HeadscaleProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"HeadscaleProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"HeadscaleProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in HeadscaleProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_TailscaleProvider_failure_no_exception(self, test_db, tmp_path):
        """TailscaleProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.tunnel_tailscale import TailscaleProvider
        provider = TailscaleProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"TailscaleProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"TailscaleProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in TailscaleProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e

    def test_provider_provider_GluetunProvider_failure_no_exception(self, test_db, tmp_path):
        """GluetunProvider.deploy() failure must return ProviderResult, not raise.
        Key regression: result.stderr on (rc, str) tuple → NameError.
        Every provider failure path must return ProviderResult(ok=False)."""
        from unittest.mock import patch, MagicMock
        from backend.infra.providers.vpn_gluetun import GluetunProvider
        provider = GluetunProvider()
        cfg = {"domain": "test.local", "network": "mediastack",
               "config_root": str(tmp_path / "config")}
        fail_sp = MagicMock()
        fail_sp.returncode = 1
        fail_sp.stdout = ""
        fail_sp.stderr = "Error: image pull failed"
        with patch("subprocess.run", return_value=fail_sp):
            try:
                result = provider.deploy(cfg)
                # Must return a result object, not raise
                assert not result.ok, (
                    f"GluetunProvider returned ok=True on compose failure"
                )
                assert result.message or result.detail, (
                    f"GluetunProvider failure message is empty"
                )
            except NameError as e:
                raise AssertionError(
                    f"NameError in GluetunProvider failure path: {e}. "
                    "Bug: result.stderr called on (rc, str) tuple — "
                    "compose_up returns a tuple not an object."
                ) from e


class TestGeneratedManifests:
    """Auto-generated schema validation tests for all 56 app manifests.
Verifies: required fields, valid port specs, valid post_deploy step types."""

    def test_manifest_manifest_actual_budget_schema_valid(self):
        """Manifest actual_budget (Actual Budget) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/actual_budget.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/actual_budget.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest actual_budget missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest actual_budget: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest actual_budget: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_adguard_home_schema_valid(self):
        """Manifest adguard_home (AdGuard Home) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/adguard_home.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/adguard_home.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest adguard_home missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest adguard_home: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest adguard_home: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_affine_schema_valid(self):
        """Manifest affine (AFFiNE) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/affine.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/affine.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest affine missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest affine: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest affine: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_audiobookshelf_schema_valid(self):
        """Manifest audiobookshelf (Audiobookshelf) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/audiobookshelf.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/audiobookshelf.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest audiobookshelf missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest audiobookshelf: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest audiobookshelf: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_bazarr_schema_valid(self):
        """Manifest bazarr (Bazarr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/bazarr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/bazarr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest bazarr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest bazarr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest bazarr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_bentopdf_schema_valid(self):
        """Manifest bentopdf (BentoPDF) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/bentopdf.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/bentopdf.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest bentopdf missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest bentopdf: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest bentopdf: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_beszel_schema_valid(self):
        """Manifest beszel (Beszel) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/beszel.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/beszel.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest beszel missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest beszel: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest beszel: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_booklore_schema_valid(self):
        """Manifest booklore (Booklore) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/booklore.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/booklore.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest booklore missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest booklore: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest booklore: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_changedetection_schema_valid(self):
        """Manifest changedetection (Changedetection.io) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/changedetection.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/changedetection.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest changedetection missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest changedetection: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest changedetection: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_crowdsec_schema_valid(self):
        """Manifest crowdsec (CrowdSec) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/crowdsec.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/crowdsec.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest crowdsec missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest crowdsec: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest crowdsec: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_ddns_updater_schema_valid(self):
        """Manifest ddns_updater (DDNS Updater) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/ddns_updater.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/ddns_updater.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest ddns_updater missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest ddns_updater: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest ddns_updater: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_decypharr_schema_valid(self):
        """Manifest decypharr (Decypharr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/decypharr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/decypharr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest decypharr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest decypharr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest decypharr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_docker_socket_proxy_schema_valid(self):
        """Manifest docker_socket_proxy (Docker Socket Proxy) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/docker_socket_proxy.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/docker_socket_proxy.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest docker_socket_proxy missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest docker_socket_proxy: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest docker_socket_proxy: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_dockge_schema_valid(self):
        """Manifest dockge (Dockge) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/dockge.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/dockge.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest dockge missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest dockge: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest dockge: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_dockhand_schema_valid(self):
        """Manifest dockhand (Dockhand) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/dockhand.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/dockhand.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest dockhand missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest dockhand: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest dockhand: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_donetick_schema_valid(self):
        """Manifest donetick (Donetick) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/donetick.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/donetick.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest donetick missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest donetick: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest donetick: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_dozzle_schema_valid(self):
        """Manifest dozzle (Dozzle) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/dozzle.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/dozzle.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest dozzle missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest dozzle: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest dozzle: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_dumb_schema_valid(self):
        """Manifest dumb (DUMB) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/dumb.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/dumb.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest dumb missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest dumb: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest dumb: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_emby_schema_valid(self):
        """Manifest emby (Emby Server) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/emby.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/emby.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest emby missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest emby: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest emby: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_filebrowser_schema_valid(self):
        """Manifest filebrowser (Filebrowser) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/filebrowser.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/filebrowser.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest filebrowser missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest filebrowser: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest filebrowser: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_fileflows_schema_valid(self):
        """Manifest fileflows (FileFlows) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/fileflows.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/fileflows.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest fileflows missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest fileflows: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest fileflows: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_freshrss_schema_valid(self):
        """Manifest freshrss (FreshRSS) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/freshrss.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/freshrss.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest freshrss missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest freshrss: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest freshrss: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_glance_schema_valid(self):
        """Manifest glance (Glance) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/glance.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/glance.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest glance missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest glance: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest glance: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_immich_schema_valid(self):
        """Manifest immich (Immich) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/immich.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/immich.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest immich missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest immich: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest immich: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_jellyfin_schema_valid(self):
        """Manifest jellyfin (Jellyfin) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/jellyfin.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/jellyfin.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest jellyfin missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest jellyfin: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest jellyfin: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_kavita_schema_valid(self):
        """Manifest kavita (Kavita) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/kavita.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/kavita.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest kavita missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest kavita: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest kavita: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_komodo_schema_valid(self):
        """Manifest komodo (Komodo) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/komodo.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/komodo.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest komodo missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest komodo: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest komodo: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_lidarr_schema_valid(self):
        """Manifest lidarr (Lidarr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/lidarr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/lidarr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest lidarr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest lidarr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest lidarr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_llamacpp_server_schema_valid(self):
        """Manifest llamacpp_server (llama.cpp Server) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/llamacpp_server.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/llamacpp_server.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest llamacpp_server missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest llamacpp_server: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest llamacpp_server: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_localai_schema_valid(self):
        """Manifest localai (LocalAI) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/localai.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/localai.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest localai missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest localai: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest localai: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_mealie_schema_valid(self):
        """Manifest mealie (Mealie) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/mealie.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/mealie.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest mealie missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest mealie: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest mealie: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_memos_schema_valid(self):
        """Manifest memos (Memos) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/memos.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/memos.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest memos missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest memos: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest memos: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_mylar3_schema_valid(self):
        """Manifest mylar3 (Mylar3) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/mylar3.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/mylar3.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest mylar3 missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest mylar3: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest mylar3: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_netdata_schema_valid(self):
        """Manifest netdata (Netdata) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/netdata.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/netdata.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest netdata missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest netdata: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest netdata: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_ntfy_schema_valid(self):
        """Manifest ntfy (ntfy) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/ntfy.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/ntfy.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest ntfy missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest ntfy: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest ntfy: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_ollama_schema_valid(self):
        """Manifest ollama (Ollama) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/ollama.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/ollama.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest ollama missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest ollama: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest ollama: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_paperless_ngx_schema_valid(self):
        """Manifest paperless_ngx (Paperless-ngx) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/paperless_ngx.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/paperless_ngx.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest paperless_ngx missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest paperless_ngx: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest paperless_ngx: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_plex_schema_valid(self):
        """Manifest plex (Plex Media Server) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/plex.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/plex.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest plex missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest plex: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest plex: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_prowlarr_schema_valid(self):
        """Manifest prowlarr (Prowlarr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/prowlarr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/prowlarr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest prowlarr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest prowlarr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest prowlarr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_radarr_schema_valid(self):
        """Manifest radarr (Radarr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/radarr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/radarr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest radarr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest radarr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest radarr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_readarr_schema_valid(self):
        """Manifest readarr (Readarr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/readarr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/readarr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest readarr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest readarr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest readarr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_sabnzbd_schema_valid(self):
        """Manifest sabnzbd (SABnzbd) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/sabnzbd.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/sabnzbd.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest sabnzbd missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest sabnzbd: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest sabnzbd: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_scrutiny_schema_valid(self):
        """Manifest scrutiny (Scrutiny) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/scrutiny.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/scrutiny.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest scrutiny missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest scrutiny: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest scrutiny: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_seerr_schema_valid(self):
        """Manifest seerr (Seerr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/seerr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/seerr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest seerr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest seerr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest seerr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_silverbullet_schema_valid(self):
        """Manifest silverbullet (SilverBullet) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/silverbullet.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/silverbullet.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest silverbullet missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest silverbullet: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest silverbullet: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_snapraid_ui_schema_valid(self):
        """Manifest snapraid_ui (SnapRAID UI) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/snapraid_ui.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/snapraid_ui.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest snapraid_ui missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest snapraid_ui: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest snapraid_ui: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_sonarr_schema_valid(self):
        """Manifest sonarr (Sonarr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/sonarr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/sonarr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest sonarr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest sonarr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest sonarr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_speedtest_tracker_schema_valid(self):
        """Manifest speedtest_tracker (Speedtest Tracker) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/speedtest_tracker.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/speedtest_tracker.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest speedtest_tracker missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest speedtest_tracker: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest speedtest_tracker: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_stirling_pdf_schema_valid(self):
        """Manifest stirling_pdf (Stirling-PDF) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/stirling_pdf.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/stirling_pdf.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest stirling_pdf missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest stirling_pdf: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest stirling_pdf: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_syncthing_schema_valid(self):
        """Manifest syncthing (Syncthing) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/syncthing.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/syncthing.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest syncthing missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest syncthing: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest syncthing: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_tdarr_schema_valid(self):
        """Manifest tdarr (Tdarr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/tdarr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/tdarr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest tdarr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest tdarr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest tdarr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_umami_schema_valid(self):
        """Manifest umami (Umami) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/umami.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/umami.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest umami missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest umami: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest umami: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_vaultwarden_schema_valid(self):
        """Manifest vaultwarden (Vaultwarden) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/vaultwarden.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/vaultwarden.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest vaultwarden missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest vaultwarden: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest vaultwarden: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_vikunja_schema_valid(self):
        """Manifest vikunja (Vikunja) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/vikunja.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/vikunja.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest vikunja missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest vikunja: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest vikunja: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_whisparr_schema_valid(self):
        """Manifest whisparr (Whisparr) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/whisparr.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/whisparr.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest whisparr missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest whisparr: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest whisparr: unknown post_deploy step type: {step.get('type')}"
            )

    def test_manifest_manifest_zilean_schema_valid(self):
        """Manifest zilean (Zilean) must have required fields and valid structure."""
        import yaml as _yaml
        fpath = REPO / "catalog/apps/zilean.yaml"
        assert fpath.exists(), f"Manifest file missing: catalog/apps/zilean.yaml"
        data = _yaml.safe_load(fpath.read_text())
        assert isinstance(data, dict), "Manifest must be a YAML dict"
        # Required fields
        for field in ("display_name", "category", "image"):
            assert field in data, (
                f"Manifest zilean missing required field: {field}"
            )
        # Image must not be empty
        assert data.get("image"), f"Manifest zilean: image is empty"
        # Ports can be list [{host_port:N}] or dict {name: int/list}
        if "ports" in data:
            ports = data["ports"]
            if isinstance(ports, list):
                for p in ports:
                    if isinstance(p, dict) and "host_port" in p:
                        assert isinstance(p["host_port"], int)
            elif isinstance(ports, dict):
                for name, val in ports.items():
                    assert isinstance(val, (int, list)), f"Port {name!r} must be int or list"
        # post_deploy steps must be valid types
        valid_step_types = {"wait_healthy", "api_ready", "wire", "smoke_test", "run"}
        for step in data.get("post_deploy", []):
            assert step.get("type") in valid_step_types, (
                f"Manifest zilean: unknown post_deploy step type: {step.get('type')}"
            )


class TestGeneratedSteps:
    """Auto-generated behavioral tests for wizard step functions.
Verifies: steps return StepResult (never raise), .ok and .step fields exist."""

    def test_step_step_system_eval_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_system_eval must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_system_eval
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_system_eval(inp)
                assert hasattr(result, "ok"), (
                    f"step_system_eval must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_system_eval must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_system_eval raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_preflight_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_preflight must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_preflight
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_preflight(inp)
                assert hasattr(result, "ok"), (
                    f"step_preflight must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_preflight must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_preflight raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_network_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_network must return StepResult with ok field.
        Risk=high. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_network
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_network(inp)
                assert hasattr(result, "ok"), (
                    f"step_network must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_network must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_network raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_config_dirs_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_config_dirs must return StepResult with ok field.
        Risk=high. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_config_dirs
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_config_dirs(inp)
                assert hasattr(result, "ok"), (
                    f"step_config_dirs must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_config_dirs must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_config_dirs raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_traefik_config_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_traefik_config must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_traefik_config
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_traefik_config(inp)
                assert hasattr(result, "ok"), (
                    f"step_traefik_config must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_traefik_config must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_traefik_config raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_traefik_deploy_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_traefik_deploy must return StepResult with ok field.
        Risk=critical. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_traefik_deploy
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_traefik_deploy(inp)
                assert hasattr(result, "ok"), (
                    f"step_traefik_deploy must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_traefik_deploy must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_traefik_deploy raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    @pytest.mark.skip(reason="Requires live Docker")
    def test_step_step_traefik_healthy_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_traefik_healthy must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_traefik_healthy
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_traefik_healthy(inp)
                assert hasattr(result, "ok"), (
                    f"step_traefik_healthy must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_traefik_healthy must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_traefik_healthy raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_complete_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_complete must return StepResult with ok field.
        Risk=critical. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_complete
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_complete(inp)
                assert hasattr(result, "ok"), (
                    f"step_complete must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_complete must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_complete raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_docker_check_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_docker_check must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_docker_check
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_docker_check(inp)
                assert hasattr(result, "ok"), (
                    f"step_docker_check must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_docker_check must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_docker_check raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_dns_validation_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_dns_validation must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_dns_validation
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_dns_validation(inp)
                assert hasattr(result, "ok"), (
                    f"step_dns_validation must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_dns_validation must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_dns_validation raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_write_env_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_write_env must return StepResult with ok field.
        Risk=high. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_write_env
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_write_env(inp)
                assert hasattr(result, "ok"), (
                    f"step_write_env must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_write_env must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_write_env raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_persist_settings_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_persist_settings must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_persist_settings
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_persist_settings(inp)
                assert hasattr(result, "ok"), (
                    f"step_persist_settings must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_persist_settings must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_persist_settings raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_deploy_infra_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_deploy_infra must return StepResult with ok field.
        Risk=critical. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_deploy_infra
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_deploy_infra(inp)
                assert hasattr(result, "ok"), (
                    f"step_deploy_infra must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_deploy_infra must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_deploy_infra raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    @pytest.mark.skip(reason="Requires live Docker")
    def test_step_step_verify_running_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_verify_running must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_verify_running
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_verify_running(inp)
                assert hasattr(result, "ok"), (
                    f"step_verify_running must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_verify_running must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_verify_running raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e

    def test_step_step_socket_proxy_result_has_ok_field(self, test_db, tmp_path):
        """Wizard step step_socket_proxy must return StepResult with ok field.
        Risk=medium. Step must NEVER raise an uncaught exception — 
        all failures must be returned as StepResult(ok=False)."""
        from backend.core import state as _sm
        _sm.configure(test_db)  # point to test DB
        from backend.platform.wizard import WizardInput, step_socket_proxy
        from unittest.mock import patch, MagicMock
        inp = WizardInput(
            domain="step-test.local",
            config_root=str(tmp_path / "config"),
            media_root=str(tmp_path / "media"),
            puid=1000, pgid=1000, timezone="UTC",
        )
        # Mock all external calls so step exercises pure logic
        sp_ok = MagicMock(); sp_ok.returncode = 0; sp_ok.stdout = "ok"; sp_ok.stderr = ""
        with patch("subprocess.run", return_value=sp_ok):
            try:
                result = step_socket_proxy(inp)
                assert hasattr(result, "ok"), (
                    f"step_socket_proxy must return StepResult with .ok field, got: {type(result)}"
                )
                assert hasattr(result, "step"), (
                    f"step_socket_proxy must return StepResult with .step field"
                )
                # ok=False is acceptable — step may have prerequisites not met
                # What is NOT acceptable: raising an exception
            except SystemExit:
                pass  # Some steps may sys.exit on critical failure
            except Exception as _e:
                raise AssertionError(
                    f"step_socket_proxy raised {type(_e).__name__}: {_e}. "
                    "Wizard steps must NEVER raise — return StepResult(ok=False) instead."
                ) from _e


class TestGeneratedTools:
    """Auto-generated run tests for operational tools.
Verifies: tools execute without crashing."""

    def test_tool_tool_ms_check_runs_without_error(self):
        """Tool ms-check must run without crashing (exit 0 or non-5x)."""
        import subprocess, sys
        tool_path = REPO / "ms-check"
        if not tool_path.exists():
            pytest.skip("Tool ms-check not found")
        r = subprocess.run(
            ["bash", "-n", str(tool_path)],
            capture_output=True, text=True, timeout=10,
        )
        assert r.returncode in (0, 1, 2), (
            f"Tool ms-check exited with code {r.returncode}. stderr: {r.stderr[:200]}"
        )

    def test_tool_tool_ms_update_runs_without_error(self):
        """Tool ms-update must run without crashing (exit 0 or non-5x)."""
        import subprocess, sys
        tool_path = REPO / "ms-update"
        if not tool_path.exists():
            pytest.skip("Tool ms-update not found")
        r = subprocess.run(
            ["bash", "-n", str(tool_path)],
            capture_output=True, text=True, timeout=10,
        )
        assert r.returncode in (0, 1, 2), (
            f"Tool ms-update exited with code {r.returncode}. stderr: {r.stderr[:200]}"
        )

    def test_tool_tool_ms_audit_runs_without_error(self):
        """Tool ms-audit must run without crashing (exit 0 or non-5x)."""
        import subprocess, sys
        tool_path = REPO / "ms-audit"
        if not tool_path.exists():
            pytest.skip("Tool ms-audit not found")
        r = subprocess.run(
            ["bash", "-n", str(tool_path)],
            capture_output=True, text=True, timeout=10,
        )
        assert r.returncode in (0, 1, 2), (
            f"Tool ms-audit exited with code {r.returncode}. stderr: {r.stderr[:200]}"
        )

    def test_tool_tool_ms_coverage_runs_without_error(self):
        """Tool ms-coverage must run without crashing (exit 0 or non-5x)."""
        import subprocess, sys
        tool_path = REPO / "ms-coverage"
        if not tool_path.exists():
            pytest.skip("Tool ms-coverage not found")
        r = subprocess.run(
            ["bash", "-n", str(tool_path)],
            capture_output=True, text=True, timeout=10,
        )
        assert r.returncode in (0, 1, 2), (
            f"Tool ms-coverage exited with code {r.returncode}. stderr: {r.stderr[:200]}"
        )

    def test_tool_tool_ms_test_py_runs_without_error(self):
        """Tool ms-test.py must run without crashing (exit 0 or non-5x)."""
        import subprocess, sys
        tool_path = REPO / "ms-test.py"
        if not tool_path.exists():
            pytest.skip("Tool ms-test.py not found")
        r = subprocess.run(
            [sys.executable, str(tool_path), "--help"],
            capture_output=True, text=True, timeout=30, cwd=str(REPO),
        )
        assert r.returncode in (0, 1, 2), (
            f"Tool ms-test.py exited with code {r.returncode}. stderr: {r.stderr[:200]}"
        )