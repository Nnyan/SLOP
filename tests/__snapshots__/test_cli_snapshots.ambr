# serializer version: 1
# name: test_ms_enforce_list_rules
  '''
  
    Active Core Rules (76 total)
  
    ✓ [crit] catalog: web_port → traefik block required
    ✓ [crit] catalog: key + display_name + category + image required
    ✓ [crit] install-custom: saved manifest must include traefik + health + li
    ✓ [crit] install-from-github: manifest key must be sanitized before use as
    ✓ [crit] executor: compose failure must set result.ok=False
    ✓ [crit] executor: install lock must always be released after install_app(
    ✓ [crit] executor: status must never be 'installing' after install_app() r
    ✓ [crit] platform: exactly one row in platform table at all times
    ✓ [crit] platform: status='ready' implies domain is non-null
    ✓ [crit] providers: compose failure must return ProviderResult, not raise
    ✓ [crit] traefik: docker socket mount must be :ro
    ✓ [crit] browser: every named Vue view has a Playwright test
    ✓ [crit] fuzzing: zero 500 responses on adversarial inputs (Schemathesis)
    ✓ [crit] mutation: critical modules must have ≥80% mutation kill rate
    ✓ [crit] security: user-supplied strings sanitized before filesystem/DB us
    ✓ [crit] endpoints: zero-data / fresh-install state must not return 500
    ✓ [crit] resources: port/key conflicts validated before allocation attempt
    ✓ [crit] generators: ms-testgen output must be syntactically valid Python
    ✓ [crit] mutmut: executor.py must have >=80% mutation kill rate
    ✓ [crit] mutmut: state.py must have >=80% mutation kill rate
    ✓ [crit] playwright: UI values verified against API responses (TestUIMatch
    ✓ [crit] tests: paths checked in tests match paths written by production c
    ✓ [crit] test: compose fragment write->read cycle (Core Rule 2.8)
    ✓ [crit] test: table name constants — no literal strings in tests (Core Ru
    ✓ [crit] Core Rule 2.8: tests checking existence must exercise the write s
    ✓ [crit] Core Rule 2.9: every state-dependent test declares preconditions 
    ✓ [crit] all data/*.json files have write→read schema tests
    ✓ [crit] ms-enforce: single command that proves all Core Rules are satisfi
    ✓ [high] catalog: web_port → health checks required
    ✓ [high] catalog: category must be in VALID_CATEGORIES
    ✓ [high] install-from-github: fetched YAML must have a size limit (64 KB)
    ✓ [high] install_instance: host_port_override must be checked for conflict
    ✓ [high] executor: install fail before deploy → DB record deleted for clea
    ✓ [high] platform: self-heal can demote ready→pending, never promote pendi
    ✓ [high] schema: pending_fixes table must be in schema.sql, not inline
    ✓ [high] statedb: db._c.commit() must never be called directly
    ✓ [high] regression: real session recordings replay without status changes
    ✓ [high] ai: ms-audit --improve reads mutation survivors + schemathesis fi
    ✓ [high] health: cycle only checks apps with status='running'
    ✓ [medi] health: every error check result must have a non-empty summary
    ✓ [high] security: externally-fetched content must have a size limit (64 K
    ✓ [high] idempotency: GET requests must never promote state
    ✓ [high] db: multi-table writes that must succeed together use one StateDB
    ✓ [high] generators: REPO must use Path(__file__).resolve().parent for sym
    ✓ [high] tests: fixtures are self-contained and always clean up state_mod
    ✓ [high] playwright: every named Vue view has a browser smoke test
    ✓ [high] keploy: key user flows have recorded session regression tests
    ✓ [high] ms-audit: Core Rules + tool outputs auto-ingested before every LL
    ✓ [high] semgrep: every rule verified against known instance before activa
    ✓ [medi] hypothesis: failure database tracked in git for cross-run accumul
    ✓ [high] pip-audit: Python CVEs checked on every ms-check (not just weekly
    ✓ [high] tests: no hardcoded absolute <REPO> paths in test files
    ✓ [high] test: tool findings schema matches what LLM context assembler rea
    ✓ [high] ms-enforce: Tier 1 checks precondition system is installed and wo
    ✓ [high] shellcheck: structural shell script bugs caught before runtime
    ✓ [high] ms-postmortem: unified bug→enforcement pipeline for new issues
    ✓ [medi] ms-setup-tools: single command installs entire testing stack
    ✓ [high] bare db._c.commit() removed from health.py, registry.py, routing.
    ✓ [high] backend/ passes mypy --strict with project config
    ✓ [medi] post-cutoff commits follow Conventional Commits 1.0
    ✓ [medi] backend/ stays within ruff.toml mccabe max-complexity (15 phase-1
    ✓ [medi] stable outputs (CLI banners, JSON shapes) covered by syrupy snaps
    ✓ [medi] tests in the order-stable subset pass under arbitrary order
    ✓ [medi] architectural decisions live as ADRs in docs/adr/
    ✓ [medi] mutating endpoints carry @limiter.limit per tier table
    ✓ [medi] tests mock at the system boundary, not the project boundary
    ✓ [medi] backend/ uses structlog with stable schema + correlation IDs
    ✓ [medi] backend/ has no functions/classes/vars unreachable via static ana
    ✓ [high] every application route is dual-mounted at /api/v1/<area> + legac
    ✓ [medi] /metrics exposes Prometheus-format metrics with bounded-cardinali
    ✓ [medi] K8s-style probes /healthz /readyz /startupz with distinct semanti
    ✓ [high] every mutating HTTP request writes one row to audit_log
    ✓ [medi] Repository Structure Discipline
    ✓ [medi] Structural Anti-Pattern Discipline
    ✓ [high] Rule-Addition Contract Discipline
    ✓ [high] Installer Layout Discipline
  
  '''
# ---
# name: test_ms_test_help_block
  '''
  usage: ms-test.py [-h] [--url URL] [--section SECTION] [--json]
                    [--destructive] [--self-improve] [--apply]
                    [--model {opus,sonnet,haiku}] [--analyze-tests] [--trend]
  
  Mediastack comprehensive test suite
  
  options:
    -h, --help            show this help message and exit
    --url URL             API base URL
    --section SECTION     Run only these sections (e.g. A,B,F,M,N)
    --json                Machine-readable JSON output
    --destructive         Include tests that modify data (install, wizard, etc.)
    --self-improve        Generate improvements via AI based on run history
    --apply               Apply self-improvements to ms-test.py (use with
                          --self-improve)
    --model {opus,sonnet,haiku}
                          AI model for --self-improve (default: sonnet)
    --analyze-tests       Analyze existing pytest suite for quality and gaps
    --trend               Show pass/fail trends from run history
  
  '''
# ---
# name: test_ms_update_help_banner
  '''
  Usage: sudo ms-update [--full] [--tests]
    (no flags)  Standard update with smart recommendations
    --full      Update + run ALL test scripts immediately
    --tests     Skip update, run test scripts only
  
  '''
# ---
