"""
tools.ms_deps — Automated dependency refresh train utilities for SLOP.

Modules:
  diff      — Compare two uv.lock files and report version changes.
  refresh   — Run uv lock --upgrade and gate on pip-audit results.
  changelog — Fetch PyPI/GitHub changelog snippets for bumped packages.
"""
