"""tools.ms_deps — Dependency management helpers for SLOP.

Modules:
    diff      — Compute version diffs between two uv.lock files.
    refresh   — Run ``uv lock --upgrade`` with audit-regression abort.
    changelog — Fetch per-package CHANGELOG snippets from PyPI / GitHub.
"""
