ebad9c4dbbe9aa8c8f3e4cf09e6c11b7e25b2c89
# origin/main SHA docs/MANAGER-HANDOFF.md was last refreshed against (2026-05-30
# clean-slate handoff). check_handoff_freshness reads this vs git rev-parse origin/main;
# it legitimately reads DRIFT for one commit after this lands (a file cannot hold its own
# commit's SHA) — the designed refresh nudge. merge_wave_to_main.py auto-stamps at the next merge.
