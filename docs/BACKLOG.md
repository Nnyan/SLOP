# SLOP Backlog

Single source of truth for known-but-not-yet-scheduled work in SLOP. Drafted
2026-05-28 via a manual sweep of all "floating work" sources (archived Robot
observations, wave-file out-of-scope sections, code TODO/FIXME markers,
self-TODO comments in configs, broken doc references, CHANGELOG mentions).

Going forward: the `tools/audit-*` family planned in S-56 Stream E will keep
this file in sync mechanically. Until then, this is a manually maintained list.

## Status legend

- `[ ]` open, unscheduled
- `[→ S-NN]` scheduled into a wave (link target)
- `[x]` done (kept for provenance; prune after 60 days)
- `[—]` won't fix / superseded (with reason)

---

## Already scheduled (cross-reference only)

- `[→ S-55-A]` bcrypt 5.x cap review — lift `<5` if no breaking changes in our usage. Source: S-46-A decision archive (run-archive; untracked).
- `[→ S-55-B]` Migrate the top YES rules from `docs/RULES-TO-TESTS-AUDIT.md` (S-50 deliverable). Source: S-50 Stream C output.
- `[→ S-55-C]` Build `tools/validate-wave-file.py` to catch wave-design errors like S-47's inverted compose labels. Source: feedback-robot-design-decisions memory.
- `[→ S-56-A]` Robot retro tooling (`tools/robot-retro.py`). Source: Round-2 retro discussion 2026-05-28.
- `[→ S-56-B]` Settings hygiene review (audit allow/deny under bypassPermissions). Source: Round-2 retro 2026-05-28.
- `[→ S-56-C]` Doc audit expansion (extend `check_referenced_files.py` to `docs/`). Source: S-48 out-of-scope.
- `[→ S-56-D]` Permanent Robot test battery + CI integration. Source: Battery 2 doctrine update 2026-05-28.
- `[→ S-56-E (proposed)]` BACKLOG audit tooling (`tools/audit_todos.py`, `audit-archived-observations.py`, `audit-wave-out-of-scope.py`) + ms-enforce check that warns on un-tracked TODOs. Source: this file's creation 2026-05-28.
- `[→ S-57-A]` Inventory the 9 FSM + 3 snapshot pre-existing failures. Source: 4 archived observations (`.claude/run-archive/2026-05-28-round2/observations/S-{48,49}-*-1.md`).
- `[→ S-57-B]` Fix 9 FSM tests in `tests/test_fsm_platform.py::TestT3ReadyToPendingReset`. Source: same.
- `[→ S-57-C]` Fix 3 snapshot tests: `test_health_summary_shape`, `test_health_scheduler_shape`, `test_health_scheduler_summary_shape_when_present` in `tests/test_snapshots.py`. Source: same.

---

## Open, unscheduled

### From wave out-of-scope sections

- `[park]` **[doctrine] Provenance headers for generated files.** **Trigger:** re-eval after S-55-B rules-to-tests migration lands and CLAUDE.md thins. Add `tools/ms-enforce check_provenance` requiring "AUTO-GENERATED by ..." headers on lockfiles, baselines, coverage maps, etc. Source: `.claude/waves/S-50-MENTAL-MODEL-FOUNDATIONS.md` Out of scope. Date added: 2026-05-28.
- `[park]` **[doctrine] Split `CLAUDE.md` into root + `docs/project-facts/`.** **Trigger:** re-eval after S-55-B rules-to-tests migration lands. Defer until rules-to-tests migration (S-55-B) thins CLAUDE.md enough that the split structure is clear. Source: `.claude/waves/S-50-MENTAL-MODEL-FOUNDATIONS.md` Out of scope. Date added: 2026-05-28.
- `[park]` **[infra] Auto-bisect on dependency-refresh-train failure.** **Trigger:** re-eval after refresh-train has been firing for 30+ days; first ambiguous-root-cause failure prompts wave drafting. If S-49's refresh PR fails tests, bisect over individual package bumps to find the offender. Originally tagged "candidate for a future S-52 wave" in the S-49 wave file (the S-52 number was unused; rename or reassign). Source: `.claude/waves/S-49-DEP-REFRESH-TRAIN.md` Out of scope.
- `[park]` **[infra] Pre-commit hook for the file-size ratchet.** Decision was CI-only for now; revisit if local-loop friction warrants. Source: `.claude/waves/S-45-RATCHET.md` Out of scope. Trigger: any contributor reports surprise on a CI-only ratchet failure.

### From archived observations

- `[x]` **[hygiene] Add `pip-audit>=2.0` to `requirements-dev.txt`.** S-49 refresh workflow degrades gracefully without it but the audit-regression-abort feature isn't fully operational in CI until installed. Source: S-49-B-2 observation (run-archive; untracked). Date added: 2026-05-28. **Done 2026-05-29:** pip-audit was already in requirements-dev.txt (prior wave) but not installed in venv; installed via `uv pip install pip-audit` (v2.10.0). First audit run found 1 CVE (starlette 0.52.1 BadHost — mitigated by TrustedHostMiddleware per ACCESS-REQUESTS.md `[upgrade]` entry). See `docs/ACCESS-REQUESTS.md` for the install-queue mechanism that now tracks this kind of work.
- `[park]` **[security/upstream] Track prometheus-fastapi-instrumentator for Starlette<1.0 cap removal.** 2026-05-29: empirical refresh attempt confirmed `prometheus-fastapi-instrumentator==7.1.0` (latest, March 2025) holds `starlette>=0.30.0, <1.0.0`. This is the ONLY constraint preventing Starlette upgrade past 0.52.1 in our resolution. CVE-2026-48710 fix is in Starlette 1.0.2+. Our `TrustedHostMiddleware` (S-46) fully mitigates the BadHost attack surface for SLOP regardless of underlying Starlette version (middleware runs OUTERMOST in wrap order → 400 before vulnerable URL-construction code is reached), so this is hygiene not urgency. Action when upstream lifts the cap: trigger `python3 -m tools.ms_deps.refresh --upgrade-package starlette` (S-49 train); BackLog removable when starlette upgrades cleanly. Track: https://github.com/trallnag/prometheus-fastapi-instrumentator/ — file an issue if no movement by 2026-09. Source: this session 2026-05-29.
- `[x]` **[HIGH→S-58]` 441 broad TestClient failures across ~16 test files** **Done 2026-05-29:** S-58 fixed via 52 base_url='http://localhost' fixture changes; suite 450→43. Merge `d13daf5`. — same TrustedHostMiddleware/`testserver` root cause that S-57 fixed for 12 specific tests. Affected files: test_agent_api.py, test_agent_apply.py, test_agent_integrity.py, test_api_coverage.py, test_comprehensive_contracts.py, test_deploy_tooling.py, test_e2e_flows.py, test_executor.py, test_failure_paths.py, test_fsm_app_install.py, test_generated_behavioral.py, test_install_app_refactor.py, test_install_inner_refactor.py, test_integration.py, test_runtime_bugs.py, test_wizard_contracts.py. Source: S-57-A-2 observation (run-archive; untracked). Date added: 2026-05-29. Wave drafted same day.
- `[x]` **[doc-hygiene] 88 docs/ broken-link warnings + 2 MAP coverage gaps** — surfaced by S-56-C's extended `check_referenced_files.py`. Largest category: 30+ refs to `docs/cleanup/*` (deleted post-v5.0.0). Plus missing files referenced from GLOSSARY/ADRs: CORE_RULES.md, RELEASE_PROCESS.md, KNOWN_FLAKES.md (all moved to slop-process private repo). MAP.md gaps: `docs/BACKLOG.md` and `docs/RULES-TO-TESTS-AUDIT.md` missing from MAP. Triage needed: which refs are real-missing vs historical stubs. Source: S-56-C observation (archived). Date added: 2026-05-29. **Done batch-6 (S-67, ebaf67c):** links 113→9; the 9 residual are intentional refs to slop-process-private files.
- `[x]` **[tooling] `tools/validate-wave-file.py` false positives** — flags `OPTIONAL-FILE-SIZE-REMEDIATION.md` (incomplete path fragment <!-- TEMPLATE: api/platform.py --> in illustrative prose, not a path claim), `S-46-PIN-RELAX.md` (correctly authorized + executed deletion of <!-- TEMPLATE: backend/requirements.txt --> retroactively flagged as missing), AND `S-59-ACCESS-REQUESTS-PROCESSOR.md` (claims a `/tmp/...` path in stream-deliverable prose — heuristic doesn't distinguish illustrative examples from real path claims). Same root cause: skip paths lacking top-level repo-relative dir prefix; add "delete"/"Authorized deletions"/"example"/"illustrative" to vocabulary. Sources: S-55-C observation + S-58 orchestrator residual-43 breakdown (both archived). Date added: 2026-05-29 (updated to add S-59 occurrence post-S-58). **Done batch-6 (S-67-E):** the 3 false-positive wave files now pass.

### Unmasked by S-58 (real bugs that became visible after host fix)

The S-58 TestClient/testserver sweep removed the `Host: testserver` → 400 mask. Tests now reach handlers — and 12 previously-hidden pre-existing failures surfaced. These are REAL bugs that need fixing in a future wave (candidate S-66: post-S-58-unmask cleanup):

- `[x]` **[bug] `/srv/mediastack` PermissionError in tests** — several tests assume a root-writable `/srv/mediastack` path. Now visible after host fix. Sources: S-58 orchestrator residual-43 breakdown (2026-05-29). Fix scope: either test fixture writes to a tmp path, or test setup creates `/srv/mediastack` with appropriate perms. Cross-cuts ~multiple test files. **Done batch-6 (S-66-B).**
- `[x]` **[bug] Cross-test DB pollution** — test isolation issue exposed by host-fix unmasking. One test's state leaks into another. Fix scope: review StateDB fixture scoping (per-test vs per-module vs per-session) + add explicit teardown if missing. Source: same as above. **Done batch-6 (S-66-B).**

### Pre-existing failures (A bucket from S-58 inventory — 30 failures across 13 causes)

These were in S-58's failure inventory but explicitly out-of-scope (S-57's "do NOT fix pre-existing" rule). Candidate for S-66 alongside the unmasked-12 above:

- `[x]` **[bug] Missing check_cleanup_ledger.py** — referenced from tests/ms-enforce but doesn't exist in public repo (moved to slop-process). Either implement a local version or remove the references. Source: S-58 inventory (2026-05-29). **Done batch-6 (S-66-C):** stale test removed.
- `[x]` **[bug] Traefik signature drift** — test asserts old signature; actual signature evolved. Update test or revert signature. Source: S-58 inventory. **Done batch-6 (S-66-D).**
- `[x]` **[bug] Migration 005 non-idempotency** — migration 005 fails on second apply. Should be idempotent (per `check_py_migration_api` doctrine). Source: S-58 inventory. Fix scope: add `IF NOT EXISTS` guards or check existing schema before applying changes. **Done batch-6 (S-66-C).**
- `[x]` **[bug] `ollama_url` default mismatch** — test expects one default, code uses another. Align. Source: S-58 inventory. **Done batch-6 (S-66-C).**
- `[x]` **[bug] `health try/except`** — missing or wrong exception handling in health code path (specifics in S-58-A-inventory.md). Source: S-58 inventory. **Done batch-6 (S-66-D).**
- `[x]` **[bug] +8 more A-bucket failures** — exact list in S-58-A-inventory.md (run-archive; untracked). Triage when S-66 fires. **Done batch-6 (S-66):** in-scope A-bucket fixed (45 total); residual 17 are env/Docker/gitignored-artifact/pre-existing — NOT regressions (see S-66-MERGE-1 decision).

**Suggested wave: S-66-POST-S58-UNMASK-CLEANUP** — fold the 12 unmasked + the 30 A-bucket failures into a single sweep wave. Stream A re-inventories against current main (post-batch-5), Streams B/C/D fix by cause-bucket. Don't try to do it inside batch-5; the orchestrator default correctly says "observe, do not fix" for pre-existing failures.
- `[x]` **[doctrine] Robot mode worktree-setup note about venv.** **Done 2026-05-29:** Resolved by ROBOT.md doctrine v3 subagent preamble; zero ModuleNotFoundError observations in batch-3/4/5 archives. Streams running `ms-enforce` in worktrees fail with `ModuleNotFoundError: structlog` because the worktree lacks deps. Subagent preamble now symlinks venv (doctrine v3), but the original observation flagged that future wave verification needs it too. Source: S-46-1 and S-50-1 observations (run-archive; untracked). Status: **likely already addressed by ROBOT.md doctrine v3 preamble** — verify in next Robot run that the issue is gone, then mark done.

### From code TODOs

- `[park]` **[security] `installer/state.py:196`** **Trigger:** re-eval if SLOP gains multi-user mode OR a user reports needing non-root install. — Tier 4 TODO: state file written as root with no explicit chown/chmod. Multi-user systems would need this. Source: code comment. Date added: 2026-05-28.
- `[park]` **[code-cleanup] `backend/api/apps.py:964`** — TODO referencing "future POST /api/apps/register" endpoint. Either implement the endpoint or remove the TODO. Source: code comment. Date added: 2026-05-28. **Re-eval trigger:** next code-cleanup wave. NOT addressed by S-67 (doc-ref scope only).
- `[—]` **[code-cleanup] `tools/analyze-tests.py:332`** — Originally flagged as a `# TODO: implement` function stub. **Corrected 2026-05-29:** S-56-E's `audit_todos.py` (using Python `tokenize`) verified line 332 is inside an f-string TEMPLATE for generated test stubs, not a real code TODO. Source: S-56-E-1 observation (run-archive; untracked). Won't fix — false positive in manual sweep.

### From broken references / stale docs

- `[x]` **[doc-hygiene] `docs/RELEASE_NOTES_v5_0_0.md` referenced three TODO files that never existed.** **Done 2026-05-29 (S-67-B):** Three broken refs (semver pre-release ordering, docker preflight ordering, root-owned test files) were stale — the TODO files were never created. Work items confirmed already tracked in BACKLOG (see above). RELEASE_NOTES updated to point to BACKLOG instead. Source: `docs/RELEASE_NOTES_v5_0_0.md:162-169`. Date added: 2026-05-28.

### From CHANGELOG mentions of "fix-later TODOs"

- `[x]` **[code-cleanup] CHANGELOG v4.2.0 F7: STRUCTURAL_RULES.md rule-004 entry** — **Done (provenance: present at commit 36d45be, initial public release).** Rule-004 was present in STRUCTURAL_RULES.md at public release; the "fix-later" TODO was resolved before the public repo was cut. Source: `CHANGELOG.md:38`. Date added: 2026-05-28. Resolved 2026-05-29 (S-67-A triage).
- `[—]` **[code-cleanup] CHANGELOG v4.2.0 F1/F4/F8: ledger coverage gap, structural antipatterns CI wiring, HARDENING_V4_2_AUDIT_RESULTS.md staleness** — Won't fix. These items reference tools (check_cleanup_ledger.py, check_structural_antipatterns.py) and docs (HARDENING_V4_2_AUDIT_RESULTS.md) that were moved to the slop-process private repo in commit 4b22390 when the public repo was created. The fixes are either complete in the private repo or irrelevant to the public codebase. Source: `CHANGELOG.md:38`. Date added: 2026-05-28. Resolved 2026-05-29 (S-67-A triage).

### From this session (Robot doctrine improvements)

- `[park]` **[hygiene] Periodic cleanup of `.claude/settings.local.json.bak.*` files.** (Subsumed by `tools/sanctioned/robot_settings.py` once it ships — retargeted from S-67-C to S-68 in batch-6 prep, 2026-05-29.) Lift-pattern accumulates backups; manual delete works. Could automate via post-cleanup hook. Source: this session's lift/restore cycles. Low priority. **Re-eval trigger:** if .bak files accumulate. robot_settings.py shipped but does not auto-prune .bak.
- `[park]` **[doctrine] Document the post-wave handoff pattern (operator does checkout + push) in CONTRIBUTING.md or installer/DEPENDENCIES.md.** Currently only in `feedback-robot-design-decisions` memory and inline in ROBOT.md "Post-wave operator handoff" section. New contributors won't find it without doctrine reading. Source: this session's lessons. Date added: 2026-05-28. **Re-eval trigger:** next doctrine sweep. Not confirmed addressed by S-67-B.
- `[→ S-56-D ongoing]` **[doctrine] Add `python3 -c "<multi-statement>"` to S-56 Stream D's permanent battery.** (Ongoing battery extension, not a separate wave.) Empirically prompts in older `acceptEdits` sessions (this session, 2026-05-29 ~00:40Z, one unidentified prompt auto-approved before capture). Likely silenced by fresh `bypassPermissions` like other syntax-analyzer categories, but unverified. If the battery confirms it's silent in fresh sessions, the doctrine note holds. If it leaks even in fresh sessions, add "use a temp script file instead of `python3 -c`" to command-style discipline. Source: this session 2026-05-29.
- `[→ S-56-D ongoing]` **[doctrine] Extend S-56-D permanent battery with the post-batch-3 leaked categories.** (Ongoing battery extension.) Five categories fired in this acceptEdits session that Battery 1+2 either tested-silent-in-fresh OR didn't test at all: (1) Write/heredoc to `.claude/waves/<new>.md`, (2) Write/heredoc to `.claude/run/<new>.md`, (3) `find <path> -delete` with the destructive-action analyzer detection, (4) re-confirm process_substitution `<(cmd)`, (5) re-confirm `find -delete`. The `.claude/waves/` and `.claude/run/` writes are NOT in the `.claude/` exemption list (only commands/agents/skills/worktrees are) — important gap to verify under bypassPermissions. Source: this session 2026-05-29, 5 leaked prompts post-batch-3 (`.claude/waves/S-58-*` write, `<(cmd)` diff, `find -delete` x2, `.claude/run/` mv). Doctrine updates landed same commit.
- `[x]` **[doctrine] Mechanical gate for ONE-orchestrator-per-batch rule.** (Slipped from S-56-E during batch-3; rescheduled to S-67-D.) `tools/audit_orchestrator_dispatch.py` added to S-56 Stream E scope. Scans `.claude/run-archive/*/status/` for the anti-pattern (multiple orchestrator status files within the same hour for different waves). Warn-only. Cross-references CLAUDE.md "Robot mode dispatch" rule. Source: this session 2026-05-29. **Done batch-6 (S-67-D):** tools/audit_orchestrator_dispatch.py shipped.
- `[x]` **[tooling] `tools/sanctioned/robot_settings.py` — promote the temp `/tmp/lift-push-restore.py` helper to a permanent project tool.** (Slipped from S-56-B during batch-3 → S-67-C → **retargeted to S-68-C in batch-6 prep, 2026-05-29** so the sanctioned-channel toolkit is the single owner; S-67-C deferred to avoid a two-waves-one-file collision.) Subcommands: `lift push`, `lift checkout-main`, `lift filter-branch`, `restore`, `push-then-restore`. Plus a canonical wave-mode deny profile at `.claude/settings-wave-mode-profile.json` so `restore` is deterministic instead of relying on in-memory state. Built on S-68's shared `_lift_restore` / `_audit` modules. Source: this session 2026-05-29 (after the user asked whether to keep the temp helper). **Done batch-6 (S-68):** tools/sanctioned/robot_settings.py + settings-wave-mode-profile.json shipped.

### From batch-4 agent waves (S-60/61/62)

- `[park]` **[code-cleanup] `backend/agent/scrub.py` over-redacts the bare word "stack".** The `_RE_USER` rule `\b(?:mediastack|stack)\b` rewrites any standalone "stack" (e.g. "stack trace") to `<USER>`. Errs in the privacy-safe direction so non-urgent, but degrades readability of scrubbed error text sent to cloud LLMs. Consider requiring a SLOP-context anchor before redacting "stack". Source: batch-4 S-61 code review 2026-05-29. **Re-eval trigger:** next agent-code-quality cleanup wave. NOT addressed by S-66 (test-failure cleanup; never touched scrub.py).
- `[park]` **[security] `backend/agent/scrub.py::is_external` keys off imported `_CLOUD_PROVIDERS`, not the `cloud_providers` routing param.** `_dispatch_llm_call` routes to a cloud HTTP endpoint based on its `cloud_providers` argument but decides whether to scrub via `is_external()` → `_CLOUD_PROVIDERS`. Identical today (same canonical set), but if a provider is ever added to the routing set without being in `_CLOUD_PROVIDERS`, egress would skip the scrub — a silent leak. Align the scrub decision to use the same set routing uses. Source: batch-4 S-61 code review 2026-05-29. **Re-eval trigger:** next agent-code-quality cleanup wave. NOT addressed by S-66.
- `[x]` **[doctrine] Orchestrator prompts must cite `git rev-parse origin/main`, never the local working-copy HEAD.** **Done 2026-05-29:** AUTONOMOUS-DEFAULTS entry added by LLM Review session in commit `26f5278`, merged via `93e7254`. Batch-4's prompt stated main HEAD `d34cb2a`, which was actually the unmerged `wave/S-58` tip; real `origin/main` was `ed7e130`. The orchestrator self-corrected and re-based, but the error was avoidable. Wave-prompt generation should resolve the base SHA via `git rev-parse origin/main` at write time and label it as such. Source: batch-4 wave-design session 2026-05-29.
- `[→ S-63, S-64]` **[agent] Batch 2 of the SLOP-agent expansion — now unblocked (S-60/61/62 on main @ `084d9d8`).** **Drafted 2026-05-29:** S-63-ROUTER-WIRING-AND-COST + S-64-AGENT-SAFE-AUTOFIX; firing in batch-5. Three candidate waves, none drafted yet (deliberately gated on batch-1 merge): (a) **router wiring** — route `_dispatch_llm_call` through `backend/agent/router` `select()` while PRESERVING the S-61 scrub choke-point (the `check_llm_outbound_scrubbed` gate guards this); (b) **persistent cost/success tracking** — promote `backend/agent/router/decisions.py` from structlog-only to a DB table (new migration) feeding cost-by-tier/provider; (c) **safe-tier auto-fix** — auto-trigger `apply_safe_fix` from the health cycle now that S-60's verify+backoff guardrails exist (currently human-triggered via POST `/agent/fixes/{id}/apply`). Memory: `project-agent-expansion-scope`. Source: batch-4 wave-design session 2026-05-29.

### From batch-5 retro (2026-05-29)

- `[x]` **[doctrine] Dedicated merge worktrees + detached HEAD pattern to prevent cross-session HEAD collisions.** Batch-5 hit two cross-session collisions (S-63 doctrine commit landed on wave/S-63's tip, S-64-A stray commit self-reverted) because the orchestrator and operator-assist session shared the main worktree's HEAD. The orchestrator adopted mid-run: do every wave-branch operation in a dedicated `.claude/worktrees/merge-*` worktree with detached HEAD, never in the shared main working tree. **Folded into S-69 (2026-05-29):** Stream G ships `tools/audit_merge_worktree_pattern.py` + `check_merge_worktree_pattern` (the mechanical half); Stream H writes the doctrine text into `ROBOT.md` § "Architecture" (extend the two-phase doctrine) and `AUTONOMOUS-DEFAULTS.md` § "Category: git operations" (the canonical-pattern half). Source: BATCH-5-COMPLETE.md orchestrator retro (run-archive; untracked). Date added: 2026-05-29. **Done batch-6 (S-69 G+H):** check_merge_worktree_pattern gate + ROBOT.md/AUTONOMOUS-DEFAULTS doctrine.
- `[x]` **[doctrine] Pin the A↔B contract in S-59 wave file (or processor-pattern docs).** S-59 Streams A and B were dispatched in parallel against an under-specified interface (A imported `APPLIERS` dict; B shipped standalone `apply_*` functions). The wave file's "Helper modules for the four category appliers" never pinned the exact symbol/shape, so neither stream was "wrong." Adapter was committed at merge time (`1b192d5`). **Done 2026-05-29:** added a "Processor-pattern contract" entry to `.claude/AUTONOMOUS-DEFAULTS.md` § "Category: agent / subagent coordination" requiring any wave whose parallel streams share a symbol to pin the exact symbol name + shape in the wave file's Deliverables section before dispatch (commit `doctrine: processor-pattern A<->B contract requirement (s-59 retro)`). S-68 applies it prospectively (pins the `tools/sanctioned/_lift_restore.py` + `_audit.py` public symbols); S-69 streams are one-file-each so no shared symbol. Source: S-59-AB-interface-gap decision (run-archive; untracked). Date added: 2026-05-29.

### From batch-6 prep / first sanctioned-tool merge (2026-05-29)

- `[x]` **[bug] `process_access_requests` adapters corrupt `permissions.allow`/`deny` with a dict.** In `tools/access_request_appliers.py`, `_allow_adapter` and `_deny_adapter` passed the WHOLE parsed `entry` dict to `apply_allow(entry,...)` / `apply_deny(entry,...)`, which expect a **string** — so the appliers appended the entire dict into the settings array. Found 2026-05-29 via `/doctor`: index 76 of `permissions.deny` was the dict `{… 'subject':'Bash(rm -rf *)', 'source':'S-60' …}`; Claude Code strips non-string entries at load, so it was silently ineffective. **Root cause:** residual of the S-59 A↔B interface gap — `_install_adapter`/`_upgrade_adapter` were fixed to extract `entry.get("subject")` but `_allow_adapter`/`_deny_adapter` were not. **Done 2026-05-29 (this session, fixed on sight):** (1) both adapters now extract the pattern via `_pkg_from_subject(entry["subject"])` (handles backtick-wrapped + bare subjects, mirroring install/upgrade); (2) `isinstance(entry, str)` guards added to `apply_allow`/`apply_deny` (raise `TypeError` instead of corrupting); (3) regression tests in `tests/test_access_request_appliers.py::TestAdapterPassesString` (4 tests). Malformed entry hand-removed from settings.local.json. ms-enforce green. Source: /doctor finding 2026-05-29.

- `[x]` **[tooling] `merge_wave_to_main.py` `_append_audit_entry` dedent bug.** `textwrap.dedent` fails to strip the common indent when multi-line fields (`branch_lines`, `preflight_str`) are interpolated into the f-string — the literal's indentation survives on most lines, producing a MERGE-LOG entry whose headers/bullets carry 6 leading spaces (renders as Markdown code blocks). First sanctioned-tool merge (2026-05-29, `ab716cd`) hit this; the entry was hand-corrected. S-68 Stream C refactors this tool — build the audit entry with explicit per-line formatting (no dedent on interpolated multi-line) and add a test asserting no generated-entry line starts with ≥4 spaces unless it's an intentional sub-bullet. Source: this session 2026-05-29. **Done batch-6 (operator-assist, cea63cb):** rebuilt entry flush-left, removed textwrap import, regression test added.
- `[x]` **[tooling] `merge_wave_to_main.py` skips ms-enforce when invoked from `main`.** `_run_ms_enforce` runs the check only if the current branch == the wave branch; invoked from `main` (the normal operator-assist path) it returns "skipped" and pre-flight [5/5] is a no-op — the merge proceeds with NO ms-enforce gate. Harmless for docs/wave-file branches, but defeats the pre-flight's purpose for code-bearing wave branches. S-68 Stream C: run ms-enforce branch-isolated (checkout the branch in a temp worktree) so the check runs regardless of the caller's current branch. Source: this session 2026-05-29, first sanctioned-tool merge. **Done batch-6 (operator-assist, cea63cb):** now runs branch-isolated in a temp worktree (artifact symlinks + info/exclude).

### From batch-6 retro (2026-05-29)

- `[→ next-tooling-cleanup]` **[tooling] Sanctioned-tool audit/scanner default paths are repo-root-relative → pollute the shared tree.** `tools/sanctioned/_audit.py::write_entry` defaults to the real `docs/SANCTIONED-OPS-LOG.md`, so S-68 verification runs of `robot_settings.py` wrote 9 real op entries that got committed into wave/S-68 (hand-stripped from main this session). Same class as the now-fixed settings-path bug (`target_paths["settings_local"]`). Fix scope: tests/verification must redirect the audit log (and any scanner output) to a tmp path — thread a log-path/target-paths param, or sandbox tool verification so committed audit logs never accumulate test entries. The orchestrator flagged this in `batch6-cross-wave-merge-notes.md`. Source: batch-6 morning merge 2026-05-29. Date added: 2026-05-29.
- `[park]` **[test-hygiene] Narrow S-66-B's `_isolate_config_data_dir` autouse fixture.** **Re-eval trigger:** next test-hygiene/cleanup wave, or if it causes a route-test surprise. S-66-B added a global autouse fixture (tests/conftest.py) redirecting `config.data_dir` for EVERY test; it is redundant for its stated purpose (the provider PermissionError is fixed by the product-code change) and over-broad. The orchestrator verified de-autousing leaves the full suite unchanged (17/2631) but left it as delivered (no scope-creep refactor in Robot mode). Narrow to opt-in/provider-scope or remove. Source: `S-66-MERGE-1` decision (run-archive). Date added: 2026-05-29.
- `[→ S-73]` **[doctrine+tooling] Wave-authoring rigor: per-stream model column + complexity-gated automated pre-flight.** Approved 2026-05-29. ONE wave bundling: (1) a `Model` column in the Parallelization stream table + selection rubric (Opus=judgment, Sonnet=bounded-impl default, Haiku=mechanical); (2) complexity-gated automated pre-flight — `tools/wave_complexity.py` scores a wave, orchestrator runs the matching fact-check rigor (extend `validate-wave-file.py`) and BLOCKS dispatch on FALSE claims; plus the first real `.claude/waves/_TEMPLATE.md`. Replaces the manual pre-flight done this session. Fires batch-7 (touches `validate-wave-file.py`/ROBOT.md/AUTONOMOUS-DEFAULTS — was in-flight in batch-6). Manager drafts it post-batch-6. Memory: `project-s73-wave-authoring-rigor`. Source: 2026-05-29 user request. Date added: 2026-05-29.

### Parked (re-eval triggers documented)

- `[parked]` **OPTIONAL-FILE-SIZE-REMEDIATION** Phases 2-5 of file splitting deferred from S-45-RATCHET. Re-eval date: **2026-08-27**, or earlier if any of the wave-file triggers fire (3+ ratchet-blocked files unsplit, new contributor friction, sustained low feature pressure). Source: `.claude/waves/OPTIONAL-FILE-SIZE-REMEDIATION.md`.

---

## Done (recent)

- `[x]` **[security]** Tailscale machine private key purged from git history via filter-branch + force-push. Done 2026-05-28 (commits `35512c0`, `b043af3...166b78c` rewrite, force-push to origin). Memory entry: `project-tailscale-key-leak-2026-05-28`.
- `[x]` **[infra]** Compose file swap (option-b → docker-compose.yml, original → docker-compose.dev.yml). Done 2026-05-28 commit `494e115`.
- `[x]` **[hygiene]** `backend/static/` untracked + `.gitignore` line 46 self-TODO removed. Done 2026-05-28 commit `35512c0` (the self-TODO was acted on by S-48 + final cleanup).
- `[x]` **[hygiene]** `data/tailscale/{tailscaled.log1.txt,tailscaled.log2.txt,tailscaled.state}` untracked. Done 2026-05-28.

---

## Won't fix / superseded

- `[—]` **"fix all pre-existing test failures" rule** — Tried twice (project-local memory then CLAUDE.md), reverted both times. Won't re-propose. Source: `feedback-no-fix-all-failures-rule` memory.

---

## How to add to this file

When you discover floating work — in a Robot retro, a code review, a stale
doc, an observation file — append a line to the appropriate section above
with: status tag, [category], description, source, date.

When you SCHEDULE an open item into a wave: change `[ ]` to `[→ S-NN-stream]`
and link the wave file.

When you COMPLETE an item: change to `[x]` and move to the Done section.
Prune Done items > 60 days old in the next monthly sweep.

When you decide NOT to fix: change to `[—]` and put a one-line reason.

The Done section is intentionally bounded — it's not a permanent archive,
it's a recent-history reference. The full history lives in git + the
`.claude/run-archive/` tree.
