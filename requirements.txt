# Mediastack — production dependencies
# Install: pip install -r requirements.txt
# Dev/testing deps: requirements-dev.txt

# Web framework
fastapi==0.136.1
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# HTTP client (health checks, GGUF downloads)
httpx==0.28.1

# Docker SDK
docker==7.1.0

# Config / data
pyyaml==6.0.2

# Pydantic (FastAPI dependency — pin for stability)
pydantic==2.10.4
bcrypt>=4.0.0

# Structured logging
structlog>=24

# Rate limiting
slowapi>=0.1.9

# Prometheus metrics — /metrics endpoint
prometheus-fastapi-instrumentator>=7.1
prometheus-client>=0.25
