---
wave: S-47-DOC-CONSOLIDATE
stream: B
sequence: 1
type: decision
default-applied: prefer-docs-subdir
timestamp: 2026-05-27T00:00:00Z
---

## Question / scenario

Two MIGRATION.md files exist with non-overlapping content:
- Root `MIGRATION.md` (~94 lines): covers **v3 (systemd) → v4 (Docker)** migration
- `docs/MIGRATION.md` (~128 lines): covers **v2 (msrad) → v3** migration

These are entirely different migration paths, not competing descriptions of the same topic. There
are zero overlapping sections, zero disagreeing sentences — the files are complementary.

Inbound reference count:
- `docs/MIGRATION.md`: 1 direct ref (`docs/DOCKER_INSTALL.md` line 334) + 2 bare `MIGRATION.md`
  refs in `docs/GLOSSARY.md` (lines 782, 784) which are ambiguous (no path prefix).
- Root `MIGRATION.md`: 0 direct inbound references with the root path.

## Best guess applied

Canonical home: `docs/MIGRATION.md`. Per AUTONOMOUS-DEFAULTS.md preference for docs/ subdir,
and per existing `docs/DOCKER_INSTALL.md` reference already pointing there.

Merge strategy: append the v3→v4 content from root as a **new top-level section** in
`docs/MIGRATION.md`. The two sections are titled differently and unambiguously separate:
- Section 1 (existing): "Migrating from Mediastack-RAD v2 → v3"
- Section 2 (added from root): "Migrating from Mediastack v3 (systemd) to v4 (Docker)"

No disagreements to resolve — content is additive only.

Root `MIGRATION.md` is replaced with a one-line pointer redirect.

GLOSSARY.md references to bare `MIGRATION.md` are updated to `docs/MIGRATION.md` for clarity.

## Alternatives considered

1. Keep both files separate — rejected because the task requires consolidation and the files are
   complementary enough to live together as a multi-version migration guide.
2. Put root content first (v3→v4) — rejected because docs/ already had v2→v3 content and the
   v2→v3 content is logically earlier in the version timeline.

## Morning review action

No action needed — pure additive merge, no content was dropped.
